# skillkit 交接（2026-08-07，项目状态盘点 + 交接归档）

> 用途：新会话读 §1（当前状态）+ §4（验证清单）+ §7（接续路径）三段够用。历史深度（Projects 浮层 / Skills 视图样式 / CLI project remove / M0-M3 / GUI parity 等）归档在 `docs/sessions/archive/`。
>
> **当前阶段**：本次上半场文档维护（归档旧交接 + 重写交接，提交 `051b689`）；下半场完成 **install-local 功能设计**——brainstorming → spec → 两轮 codex 对抗审查加固（9 项发现闭合）→ 实现计划，全程未写业务代码。spec/review/plan 已提交（HEAD=`49d43c3`），**实现计划就绪待执行**。`make check` 全绿。无 remote。
>
> **重要修正**：上一份交接（08-06）只记了 Skills 视图样式，**漏记了 08-04→08-07 之间两大功能线**（skill scope/profile 管理、agent 精确探测）。本交接 §2 补全盘点，作为这阶段的权威状态快照。本次 install-local 设计见 §2.4 + §7。

## 1. 当前状态

### 1.1 命令表面

CLI 顶层分组：`source` / `install` / `skill`(find/list/remove) / `upgrade` / `import-existing` / `project` / `profile` / **`rescope`（新增，scope 转移）** / `serve`。

GUI 端点（`/{token}/` 前缀）按页：sources / skills / profiles / projects(workspace) / 详见 §1.3 和各路由文件。token 每次启动随机，从启动日志 grep `[0-9a-f]{32}`。

### 1.2 结构性事实（架构层）

- **三层共享 core**：core 全部业务逻辑，cli/server 薄壳。无常驻 daemon，文件锁（`~/.skillkit/.lock`）串行化并发写。
- **单版本模型**：canonical 物理存储只有一份（`~/.skillkit/.agents/skills/<source>/<skill>/`），版本/hash 记在 registry。多版本不分目录（YAGNI）。
- **skill 双 scope**：global（全局基座，`~/.agents/skills/` symlink 落地，独立）vs local（场景/项目组合成员）。**互斥**：global 不属任何 profile、不进任何 project.installed_skills（决策 17，core 硬约束）。
- **agent 能力配置驱动**：是否 symlink、是否直读池子声明在 `config.toml`；`Config::default()` 默认 claude-code/cursor/codex 三大主流。
- **项目 agents 精确探测（决策 19，本次重点之一）**：注册/绑定/同步时按项目根目录探测——配置目录（`.claude`/`.codex`/`.cursor`/`.agents`）命中即按目录判定（多则全收），无配置目录按指令文件（`CLAUDE.md`→claude-code、`AGENTS.md`→开源标准），全未命中回退开源标准 `.agents/`（copy 落地）。探测逻辑收敛在 `crates/core/src/detect.rs`。
- **项目级 `.agents/skills/` 双角色**：shared 真实目录只读发现（归属 `agents/<name>`）；探测未命中具体 agent 时兼作开源标准 local 落地目录（参与 apply）。
- **项目 local skill 平铺落地**：`<agent>/skills/<skill>/`，不建 `local/` 子目录（Claude Code 只发现一层）；git 忽略写 `<project>/.git/info/exclude`。
- **约束红线**：`~/.agents/skills/` 只放全局公共 skill，绝不写元数据文件；元数据全收 `~/.skillkit/`；不管 Claude 原生插件和 `~/.claude/commands/`。

### 1.3 GUI 关键交互面

- **Skills 视图**：7 列表格（id/scope/source/version/hash/归属 profile/ops）；scope 过滤条（global/local/profile，`a.chip` 圆角矩形控件）；行勾选高亮 + 批量归入（assign）/移出（unassign）；scope 转移按钮（`.pill-btn`，→global 绿 / →local 中性）；unmanaged 红色 tag；hash 列 ellipsis + title。样式对齐 `demo/index.html`。
- **Projects(workspace) 视图**：路径输入 Tab 补全 + 浏览浮层 + scan 浮层 toggle；注册前 canonical 查重；「重新探测 agents」按钮（原「同步默认 agents」）；绑定 profile 前刷新探测。
- **写操作（POST/DELETE）**返回完整页面 `hx-target="body" hx-swap="outerHTML"`；SSE `changed` 事件刷新 `?fragment=1` 纯片段（带当前 query 保高亮/过滤）；scan 浮层开时前端跳过 SSE（防清浮层）。
- **错误反馈**：错误 toast（`superpowers`-style）；引导性文案（如「skill 未安装，先 install <id>」）。

### 1.4 验证 flow

```bash
cd /Users/mywo/lab/skillkit
make check                                # 全绿
make run ARGS="serve --port 7317"         # 起 GUI 手动走查（token 见启动输出）
cargo build -p skillkit-cli               # 改 CSS/模板后必跑（rust-embed 嵌入二进制）
```

## 2. 自上份完整交接（08-04）以来的改动盘点

> 08-06 交接只覆盖了第 3 段（Skills 样式），第 1/2 段漏记。此处按功能线补全，全部已提交。

### 2.1 功能线 A：skill scope/profile 管理（决策 17/18）

**core**：
- `scope.rs`：`set_scope` 实现双向转移。local→global 建 `ensure_global_claude` symlink + 清 profile/project 引用（不可逆，可重新归入）；global→local 撤链 `remove_global_claude`（不加 scope 守卫，规避先改 scope 再撤链的顺序陷阱）+ 去重（扫物理池防漂移留重复 canonical）。
- global/profile 互斥硬约束：`profile.add_skill`/`project.add_skill`/`project.set_profiles` 加 `&Registry` 参数，global 拒绝/跳过，文案引导先 rescope。
- `error.rs`：新增 `SkillIsGlobal` 变体。

**cli**：
- `rescope` 命令（`<id> <global|local>`，两方向默认交互确认，`--yes` 跳过，`--json` schema 锁定）。

**server（Skills 视图大改造）**：
- 数据层：`SkillsQuery` + 反向 map（skill→归属 profile）+ profile 过滤。
- 新端点：`assign`/`assign-new`（批量归入，global 跳过抛 scope）、`delete-profile`、`unassign`（批量移出）、`rescope`（scope 转移 GUI）。
- 视图：chips 归属列、行高亮 toggle、批量操作、profile 过滤、scope 转移按钮。
- `profiles.rs`：create 防覆盖、渲染过滤 global、去掉手填 add-skill 表单。
- bug 修复：SSE 刷新带当前 query（保 selected/profiles 高亮不丢）；按钮冒泡误触 toggleSkill；global 行 selected 死循环。

### 2.2 功能线 B：agent 精确探测（决策 19，修订决策 16）

- `core/detect.rs`：`detect_agents(project_root)` 按配置目录表（`CONFIG_DIRS`）→指令文件表（`INSTRUCTION_FILES`）→开源标准（`OPEN_STANDARD_AGENT="agents"`）回退。8 个单元测试。
- `project.rs`：`Project::refresh_agents()` 覆盖旧声明。
- `cli/project.rs`：`add`/`apply-profile` 改用 `detect_agents`（探测优先，`--agents` 显式覆盖兜底）。
- `server/projects.rs`：注册/scan 注册改用 `detect_agents`；`sync-agents` 语义从「补全到 Config 全 agent」改为「重新探测」（按钮改名「重新探测 agents」）；`set-profiles` 绑定前 `refresh_agents`，杜绝给未用 agent 建目录。
- 文档落档：决策 19 入 `docs/design-decisions-2026-07-29.md`；主 spec §6.3/§7/§8.5 修订；CLAUDE.md §5 约束更新；UI spec 注册表单去 agents 输入。

### 2.3 视觉层：Skills 视图样式对齐 demo（08-06 交接原覆盖项）

`skills_main.html`（7 列 + page-head/toolbar 重构）+ `app.css`（tag/pill-btn/cell-id/skills-table 块 + teal 色变量 + `a.chip` 圆角矩形）。已提交 `6cff0dd`。CSS 只增新 class，不碰共享 class。

### 2.4 本次会话：install-local 设计就绪（未实现）

把外部 skill 目录/zip 安装到 canonical 池、算 sha256 标记 managed、注册 `local/<name>`（伪 source，与 `unmanaged` 对称，不进 SourcesStore）。完成设计全链路，**未写业务代码**：

- **spec** `docs/superpowers/specs/2026-08-07-install-local-skill-design.md`（CLI `install local <目录|zip>` + GUI `POST /skills/install-local`）。
- **两轮 codex 对抗审查** + 全部采纳，9 项发现闭合：①name 校验放行 `.`/`..` 致 canonical 池路径逃逸（`--force` 可删光池）②`--force` 按目录名误删跨 source canonical ③zip/copy 无路径穿越·symlink·体积约束 ④hash 无定界可碰撞 ⑤`--force` 先删后拷非原子 → 二轮深化：force 三段原子 `target→.old→staging→target→rm .old`、锁 key="registry" 全程持锁（闭 lost-update，`Registry::save_raw` 让位防同进程自死锁）、force 补 registry 归属反查、hash 移 rename 前 + 两段回滚 + 孤儿目录处理。审查存档 `docs/superpowers/reviews/2026-08-07-install-local-skill-design-review.md`。
- **实现计划** `docs/superpowers/plans/2026-08-07-install-local-skill.md`：7 个 TDD 任务（deps+error / 纯函数 / hash+copy / extract_zip / install_local core+save_raw / CLI / server），每步含完整代码，无占位符。新依赖 `zip`、`sha2`（pure rust）。
- **已知限制**（spec §3.7，本功能不全包）：池物理变更方 uninstall/rescope/install-add 不持锁（grep 证实的既有债），不与 install_local 串行，全闭需池级共享锁（超本功能范围）。

## 3. 关键背景

### 3.1 scope 互斥 + 转移模型（改 Skills/Profile 必读）

global skill 是全局基座，不属 profile/project；local 才进组合。转移=改 scope + 立即同步物理落地 + 清引用。core 用 `&Registry` 校验 scope（scope 只存 registry）。详见决策 17/18。

### 3.2 agent 探测优先级与回退

探测表固定（claude/codex/cursor/agents）。配置目录优先于指令文件；多配置目录全收；全未命中回退 `.agents/`（开源标准，copy 落地）。新主流 agent 出现，在 `detect.rs` 表里加一行即可。`scan_shared` 在 `proj.agents` 各 agent 目录之外，只读发现项目级 `.agents/skills/` 共享池。

### 3.3 改 CSS/模板生效链路（高频踩坑）

改 `app.css`/Askama 模板 → `cargo build -p skillkit-cli`（rust-embed 重新打包）→ `pkill -f "target/debug/skillkit serve"` → 重启 serve → 浏览器强刷（token 变了）。不重建二进制只重启 serve，CSS/模板变更不生效。GUI 用 `skillkit serve`（cli 二进制）起，不用 `cargo run -p skillkit-server`（后者无 bin target）。

### 3.4 GUI 调试（playwright）

playwright 装在 `~/.local/pipx/venvs/playwright/bin/python`（非项目依赖）。SSE 长连接致 `wait_until="networkidle"` 永不空闲，必须用 `wait_until="load"`。截图读取偶报「Unsupported Image」，改用 DOM 断言（`getComputedStyle`）验证样式。

## 4. 验证清单（重载 / 切换后立即跑）

- [ ] `cd /Users/mywo/lab/skillkit && git status`（应干净，或仅 docs/sessions 改动）+ `make check` 全绿。
- [ ] `make run ARGS="serve --port 7317"` 起，浏览器走查四页：Skills（7 列 + scope 过滤 + 批量归入/移出 + scope 转移按钮）、Projects（Tab 补全 + 浏览/scan 浮层 + 注册查重 + 重新探测 agents）、Sources、Profiles（create 防覆盖）。
- [ ] CLI 走查：`skillkit skill list`（标记 unmanaged）/ `skillkit rescope <id> global`（确认 + 物理落地）/ `skillkit project apply-profile`（探测 agent）。
- [ ] **回归信号**：写操作返回 body outerHTML（非片段）；SSE 刷新纯片段且带 query（高亮/过滤不丢）；sources/projects/profiles 共享 class 没被改坏。
- [ ] **完成信号**：`detect.rs` 有 `detect_agents` + `CONFIG_DIRS`/`INSTRUCTION_FILES`/`OPEN_STANDARD_AGENT`；`scope.rs` 有 `set_scope`；cli 有 `rescope`；skills handler 有 assign/unassign/rescope。

## 5. 已知遗留 / 待办

1. **install-local 待实现（本次设计就绪）**：spec + 7 任务 TDD 计划已就绪（§2.4），按 `docs/superpowers/plans/2026-08-07-install-local-skill.md` 执行；superpowers:subagent-driven-development（每 task 派子 agent + 审查）或 executing-plans（内联）皆可。
2. **无 remote / 无待推**：本仓库无 git remote（`git remote -v` 为空），全部提交只在本地主分支。
3. **基建债**（跨多轮交接）：Cargo.toml `[package]` 元数据（core/cli/server 补 description/license/repository，README 已声明 MIT）；CI（GitHub Actions 跑 make check）。
4. **测试残留**：若 `huasheng-data-server` 仍登记在 `~/.skillkit`，用 `make run ARGS="project remove huasheng-data-server --yes"` 注销（只删 toml）。
5. **联网测试 ignored**：cli e2e 8 个 + core m0 2 个需真跑 npx skills（联网），`cargo test -- --ignored` 手动跑，不进 check。
6. **可选打磨**：browse 路径 percent-encode；make e2e-all；其它页面对齐 demo（Projects 卡片网格 / Sources 源注册表 / Profiles 粗分类）。更早遗留见 archive `2026-07-29-skillkit-design.md` §5。

## 6. 关键文件路径速查

```
/Users/mywo/lab/skillkit/
├── crates/
│   ├── core/src/
│   │   ├── detect.rs        # detect_agents + 探测表 —— 决策 19 核心
│   │   ├── scope.rs         # set_scope 双向转移 + 去重 —— 决策 18
│   │   ├── registry.rs      # Scope/SkillMeta + is_global/is_local
│   │   ├── profile.rs       # add_skill 加 registry 校验拒绝 global —— 决策 17
│   │   ├── project.rs       # refresh_agents/add_skill/set_profiles 校验
│   │   ├── symlink.rs       # ensure_global_claude / remove_global_claude
│   │   └── apply.rs         # build_status/compute_diff/collect_shared
│   ├── cli/src/commands/
│   │   ├── rescope.rs       # scope 转移命令 —— 决策 18
│   │   └── project.rs       # add/apply-profile 用 detect_agents
│   └── server/src/routes/
│       ├── skills.rs        # SkillsQuery/assign/unassign/rescope + SkillsMainTpl
│       ├── projects.rs      # complete/toggle/scan/detect/sync-agents(set-profiles refresh)
│       └── profiles.rs      # create 防覆盖/filter_global_skills
├── demo/index.html          # Skills 视觉参照原型
├── docs/
│   ├── 2026-07-29-skillkit-design.md      # 主 spec（§6.3/§7/§8.5 修订）
│   ├── design-decisions-2026-07-29.md     # 决策 1-19（17/18/19 为本阶段重点）
│   ├── superpowers/
│   │   ├── specs/2026-08-07-install-local-skill-design.md   # install-local 设计（两轮审查加固）
│   │   ├── plans/2026-08-07-install-local-skill.md          # install-local 7 任务 TDD 计划（待执行）
│   │   └── reviews/2026-08-07-install-local-skill-design-review.md  # 两轮 codex 对抗审查存档
│   └── sessions/
│       ├── 2026-08-07-skillkit.md         # 本交接（当前）
│       └── archive/                       # 08-02/03/04/06 + 07-29 design
└── CLAUDE.md                # §5 约束（agent 探测）/ §7.5 前端约定
```

运行时目录：`~/.skillkit/`（config/sources/registry/profiles/projects/.lock）、`~/.skillkit/.agents/skills/`（canonical 池）、`~/.agents/skills/`（global apply）。

## 7. 下次接续工作的最短路径

### 7.1 冷启动（新会话第一件事）

```bash
cd /Users/mywo/lab/skillkit
git status                                # 应干净
make check                                # 全绿
```

**必读**：§1.2（架构不变量）+ §2（两大功能线盘点）+ §3.1/§3.2（scope 互斥 + agent 探测）。若做 GUI/样式，加读 §3.3（CSS 生效链路）。

### 7.2 焦点：install-local 实现（设计就绪）

按 `docs/superpowers/plans/2026-08-07-install-local-skill.md` 7 个 TDD 任务执行（§2.4 是设计摘要）。冷启动后直接跑 Task 1（core 加 zip/sha2 依赖 + error 变体）。superpowers:subagent-driven-development 每 task 派子 agent + 审查为推荐。

**必读**：spec `docs/superpowers/specs/2026-08-07-install-local-skill-design.md`（§3.2 数据流 / §3.4 name 校验 / §3.6 安全 / §3.7 原子性是审查加固重点）。

### 7.3 次优先：基建债

1. **Cargo.toml `[package]` 元数据**：core/cli/server 补 description/license/repository。
2. **CI**（GitHub Actions）：跑 make check。

### 7.4 优先级

install-local 实现（计划就绪）→ 基建债（Cargo.toml 元数据 + CI）→ 可选打磨（browse percent-encode / 其它页面对齐 demo / make e2e-all）。

## 7.x (archive) 之前接续路径

旧接续路径（Skills 视图样式 / Projects 浮层 / CLI remove / 默认多 agent / GUI parity / M3 等）归档在 `docs/sessions/archive/` 对应交接的 §7。
