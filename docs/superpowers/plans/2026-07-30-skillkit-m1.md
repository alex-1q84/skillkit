# skillkit M1 闭环实现计划（profile / project / apply）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 M0 装好的 skill 能按项目精确、幂等地落地到各 agent 目录——profile（粗分类候选集）+ project（精确声明）+ apply（幂等落地）+ status（diff 感知）形成闭环。

**Architecture:** 三层共享 core。新增 `profile.rs` / `project.rs` / `apply.rs` 三个 core 模块，CLI 加 `profile` / `project` 两个命令组。apply = diff 计算（纯逻辑）+ 按 agent 能力落地（Claude symlink / Cursor copy，由 `config.toml` 声明）+ `.git/info/exclude` 维护 + 版本冲突检测（`locked_shas` 基线 vs `registry.commit_sha`）。复用 M0 的 `registry` / `install` / `symlink` / `config`，不重写。

**Tech Stack:** Rust edition 2021 · clap(derive) · serde + toml + serde_json · thiserror(core) + anyhow(cli) · dirs · tracing · tempfile(test) · **uuid(新增，Task 3：project-id 随机短码)** · **chrono(新增，Task 10：installed_at 真实时间)**。

## Global Constraints

（每个 task 隐含包含，来自 CLAUDE.md §5/§7/§8 + spec §6.3/§10）

- Rust edition 2021，workspace resolver = "2"。
- 路径绝不硬编码：用户目录一律 `dirs::home_dir()`，测试用 `Paths::new(tempdir)` 注入。
- `~/.agents/skills/` 只放全局公共 skill，绝不写元数据文件进去；元数据统一收 `~/.skm/`（profiles/projects 在其下）。
- core 用 `thiserror`，cli 顶层 `anyhow`；错误「反馈引导行动」，不静默跳过。
- 状态文件（profile/project）原子写：`crate::error::atomic_write`（M0 已有，复用）。
- **core 公开类型一律 `lib.rs` 完整 re-export**（M0 约定，CLAUDE.md §7）。
- **集成测试放 `crates/core/tests/`**（M0 约定），不放 workspace 根。
- **测试 git commit 带 `-c user.email -c user.name`**（M0 约定）。
- 单版本模型：canonical 物理只有一份，`locked_shas` 是上次 apply 的基线快照（非版本锁），用于检测 canonical 升级漂移。
- local skill **平铺**落地在 `<project>/<agent>/skills/<skill>/`，绝不建 `local/` 子目录（Claude 只发现一层）。
- git 忽略写 `<project>/.git/info/exclude`，不改项目 `.gitignore`。
- 项目 shared skill 只读发现（扫描展示），不安装/升级/卸载。
- local 与 shared 同名：shared（已在 git）优先，local 跳过落地并警告。
- TDD 红绿 + 每 task 一 commit（commit message 中文 Conventional Commits）。
- 日志 `tracing`，CLI 用户可见输出可用 `println!`。
- 注释中文。

## File Structure

```
skillkit/
  crates/
    core/
      Cargo.toml                  # 加 chrono 依赖（Task 10）
      src/
        lib.rs                    # 注册 profile/project/apply 模块 + re-export
        profile.rs                # Profile / ProfileStore（profiles/<name>.toml）
        project.rs                # Project（id/name/path）+ new_id/register/rebind + list_ids
        apply.rs                  # ApplyDiff 计算 + local 落地 + apply 编排
        paths.rs                  # 加 profiles_dir() / projects_dir() / project_exclude()
        install.rs                # now_iso() 改真实时间（Task 10）
      tests/
        m1_e2e.rs                 # M1 端到端（profile→project→apply→status 闭环）
    cli/
      src/
        main.rs                   # Cmd 加 Profile / Project
        commands/
          mod.rs                  # pub mod profile; pub mod project;
          profile.rs              # profile create/add-skill/remove-skill/list
          project.rs              # project add/rebind/scan/list/apply-profile/add-skill/remove-skill/apply/status
```

文件职责单一：`profile`/`project` 各管一种状态文件，`apply` 编排落地（调 registry/config/symlink），CLI 是薄壳。

---

### Task 1: profile 模块（core）

**Files:**
- Modify: `crates/core/src/paths.rs`（加 `profiles_dir()`）
- Modify: `crates/core/src/lib.rs`（注册 + re-export）
- Create: `crates/core/src/profile.rs`

**Interfaces:**
- Consumes: `Paths`, `atomic_write`, `SkillkitError`
- Produces: `Profile { name, description, skills: Vec<String> }`、`ProfileStore::load/save/get/add_skill/remove_skill/list_names`

- [ ] **Step 1: paths.rs 加 profiles_dir（写失败测试）**

`crates/core/src/paths.rs` 的 `impl Paths` 末尾追加方法，并在 `#[cfg(test)] mod tests` 的 `production_and_injected_share_layout` 断言里加一行：

```rust
    /// profile 注册表目录（~/.skm/profiles/）。
    pub fn profiles_dir(&self) -> PathBuf {
        self.skm_dir().join("profiles")
    }
```

测试断言加：
```rust
        assert_eq!(
            p.profiles_dir(),
            PathBuf::from("/tmp/fakehome/.skm/profiles")
        );
```

- [ ] **Step 2: 跑测试确认绿（paths 已有测试，新增方法直接可过）**

Run: `cargo test -p skillkit-core paths`
Expected: PASS（方法实现与测试同批写入，绿）。注：paths 是基础设施，不再走严格红绿（方法即数据），测试守护正确性。

- [ ] **Step 3: 写 profile 测试（先失败）**

`crates/core/src/profile.rs`：
```rust
//! profile（粗分类候选集）—— ~/.skm/profiles/<name>.toml，只存 skill id 列表（DRY）。
//! source/scope/version 等信息只在 registry 存一份，profile 不重复。
use crate::error::{atomic_write, Result, SkillkitError};
use crate::paths::Paths;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Profile {
    pub name: String,
    #[serde(default)]
    pub description: String,
    #[serde(default)]
    pub skills: Vec<String>,
}

impl Profile {
    pub fn load(paths: &Paths, name: &str) -> Result<Self> {
        let path = paths.profiles_dir().join(format!("{name}.toml"));
        if !path.exists() {
            return Err(SkillkitError::ProfileNotFound { name: name.to_string() });
        }
        let content = std::fs::read_to_string(&path)?;
        Ok(toml::from_str(&content)?)
    }

    pub fn save(&self, paths: &Paths) -> Result<()> {
        let dir = paths.profiles_dir();
        std::fs::create_dir_all(&dir)?;
        let path = dir.join(format!("{}.toml", self.name));
        atomic_write(&path, &toml::to_string_pretty(self)?)?;
        Ok(())
    }

    pub fn add_skill(&mut self, id: &str) -> Result<()> {
        if self.skills.iter().any(|s| s == id) {
            return Err(SkillkitError::SkillAlreadyInstalled { id: id.to_string() });
        }
        self.skills.push(id.to_string());
        Ok(())
    }

    pub fn remove_skill(&mut self, id: &str) -> Result<()> {
        let before = self.skills.len();
        self.skills.retain(|s| s != id);
        if self.skills.len() == before {
            return Err(SkillkitError::SkillNotInstalled { id: id.to_string() });
        }
        Ok(())
    }
}

/// profile 目录扫描（list 用）。
pub fn list_names(paths: &Paths) -> Result<Vec<String>> {
    let dir = paths.profiles_dir();
    if !dir.exists() {
        return Ok(Vec::new());
    }
    let mut names = Vec::new();
    for entry in std::fs::read_dir(&dir)? {
        let entry = entry?;
        let p = entry.path();
        if p.extension().and_then(|e| e.to_str()) == Some("toml") {
            if let Some(stem) = p.file_stem().and_then(|s| s.to_str()) {
                names.push(stem.to_string());
            }
        }
    }
    names.sort();
    Ok(names)
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::paths::Paths;
    use tempfile::tempdir;

    fn paths() -> Paths {
        Paths::new(tempdir().unwrap().path().to_path_buf())
    }

    #[test]
    fn add_remove_skill_persists() {
        let p = paths();
        let mut profile = Profile { name: "frontend".into(), description: String::new(), skills: vec![] };
        profile.add_skill("skills.sh/frontend-design").unwrap();
        profile.add_skill("skills.sh/dataviz").unwrap();
        assert!(profile.add_skill("skills.sh/frontend-design").is_err(), "重复 add 报错");
        profile.save(&p).unwrap();

        let reloaded = Profile::load(&p, "frontend").unwrap();
        assert_eq!(reloaded.skills, vec!["skills.sh/frontend-design", "skills.sh/dataviz"]);

        let mut reloaded = Profile::load(&p, "frontend").unwrap();
        reloaded.remove_skill("skills.sh/dataviz").unwrap();
        reloaded.save(&p).unwrap();
        assert_eq!(Profile::load(&p, "frontend").unwrap().skills, vec!["skills.sh/frontend-design"]);
    }

    #[test]
    fn load_missing_fails() {
        let p = paths();
        assert!(matches!(
            Profile::load(&p, "nope"),
            Err(SkillkitError::ProfileNotFound { .. })
        ));
    }

    #[test]
    fn list_names_sorted() {
        let p = paths();
        Profile { name: "base".into(), description: String::new(), skills: vec![] }.save(&p).unwrap();
        Profile { name: "frontend".into(), description: String::new(), skills: vec![] }.save(&p).unwrap();
        assert_eq!(list_names(&p).unwrap(), vec!["base", "frontend"]);
    }
}
```

- [ ] **Step 4: error.rs 加 ProfileNotFound 变体**

`crates/core/src/error.rs` 的 `SkillkitError` 枚举加：
```rust
    #[error("profile 不存在：{name}（先 `skillkit profile create {name}`）")]
    ProfileNotFound { name: String },
```

- [ ] **Step 5: 跑测试确认失败（Profile::load/save 等已实现但 lib 未注册模块 → 实际此处测试与实现同文件，编译即过。红绿点在 SkillkitError::ProfileNotFound 若漏加则编译失败）**

Run: `cargo test -p skillkit-core profile`
Expected: 若 Step 4 漏加 ProfileNotFound → 编译失败（红）；加了 → PASS（3 测试）。

- [ ] **Step 6: lib.rs 注册 + re-export**

`crates/core/src/lib.rs` 加 `pub mod profile;` 与 `pub use profile::{list_names as list_profile_names, Profile};`。

Run: `cargo test -p skillkit-core profile`
Expected: PASS（3 测试）。

- [ ] **Step 7: Commit**

```bash
git add crates/core/src/profile.rs crates/core/src/paths.rs crates/core/src/error.rs crates/core/src/lib.rs
git commit -m "feat(core): profile 候选集读写（profiles/<name>.toml）"
```

---

### Task 2: profile CLI

**Files:**
- Create: `crates/cli/src/commands/profile.rs`
- Modify: `crates/cli/src/commands/mod.rs`、`crates/cli/src/main.rs`

**Interfaces:**
- Consumes: `skillkit_core::{Profile, list_profile_names, paths::Paths}`
- Produces: `profile create / add-skill / remove-skill / list` 子命令

- [ ] **Step 1: 写 commands/profile.rs**

```rust
//! profile 子命令：调 core 的 Profile。
use clap::{Args, Subcommand};
use skillkit_core::{list_profile_names, paths::Paths, Profile};

#[derive(Args)]
pub struct ProfileCmd {
    #[command(subcommand)]
    cmd: ProfileSub,
}

#[derive(Subcommand)]
enum ProfileSub {
    /// 创建空 profile
    Create { name: String, #[arg(long)] description: Option<String> },
    /// profile 加 skill（id = <source>/<skill>）
    AddSkill { profile: String, id: String },
    /// profile 移除 skill
    RemoveSkill { profile: String, id: String },
    /// 列出所有 profile
    List,
}

pub fn run(cmd: ProfileCmd) -> anyhow::Result<()> {
    let paths = Paths::production();
    match cmd.cmd {
        ProfileSub::Create { name, description } => {
            Profile { name: name.clone(), description: description.unwrap_or_default(), skills: vec![] }.save(&paths)?;
            println!("✓ 已创建 profile：{name}");
        }
        ProfileSub::AddSkill { profile, id } => {
            let mut p = Profile::load(&paths, &profile)?;
            p.add_skill(&id)?;
            p.save(&paths)?;
            println!("✓ {profile} 已加 {id}");
        }
        ProfileSub::RemoveSkill { profile, id } => {
            let mut p = Profile::load(&paths, &profile)?;
            p.remove_skill(&id)?;
            p.save(&paths)?;
            println!("✓ {profile} 已移除 {id}");
        }
        ProfileSub::List => {
            for name in list_profile_names(&paths)? {
                let p = Profile::load(&paths, &name)?;
                println!("{:16} ({} skills) {}", name, p.skills.len(), p.description);
            }
        }
    }
    Ok(())
}
```

- [ ] **Step 2: 接线 main.rs + mod.rs**

`commands/mod.rs` 加 `pub mod profile;`。`main.rs` 的 `Cmd` 加 `Profile(ProfileCmd)` 分支调 `commands::profile::run(cmd)?`，顶部 `use commands::profile::ProfileCmd;`。

- [ ] **Step 3: 验证 CLI 流程**

Run: `make build && TESTHOME=$(mktemp -d) && HOME=$TESTHOME ./target/debug/skillkit profile create frontend --description 前端 && HOME=$TESTHOME ./target/debug/skillkit profile add-skill frontend dc/logseq && HOME=$TESTHOME ./target/debug/skillkit profile list && rm -rf $TESTHOME`
Expected: 打印创建/添加/列表，profile frontend 含 1 skill。

- [ ] **Step 4: Commit**

```bash
git add crates/cli/src/
git commit -m "feat(cli): profile 命令（create/add-skill/remove-skill/list）"
```

---

### Task 3: project 模块 + project-id 生成（core）

**Files:**
- Modify: `crates/core/src/paths.rs`（加 `projects_dir()`）
- Modify: `crates/core/src/lib.rs`
- Create: `crates/core/src/project.rs`

**Interfaces:**
- Consumes: `Paths`, `atomic_write`, `SkillkitError`
- Produces: `Project { id, name, path, agents, applied_profiles, installed_skills, locked_shas }`、`Project::{load, save, register, rebind, add_skill, remove_skill, apply_profile}`、`new_id() -> String`、`list_ids`

**设计决策（spec §8.5/§11 未明，本计划定档，主人确认）：** `project-id` = 注册时随机生成的 8 位 hex 短码（`uuid v4` 取前 8 位大写，形如 `A1B2C3D4`，似 git short sha），**独立于 path**——项目移动目录或改名后 id 不变。project.toml 记录实际 `path` + `name`（从 path basename，可变），`id` 注册后冻结。重绑定命令 `skillkit project rebind <id> <new-path>` 更新 path/name，id 不变。文件名 `<id>.toml`。

- [ ] **Step 1: paths.rs 加 projects_dir**

`impl Paths` 加：
```rust
    /// project 注册表目录（~/.skm/projects/）。
    pub fn projects_dir(&self) -> PathBuf {
        self.skm_dir().join("projects")
    }
```

- [ ] **Step 2: 加 uuid 依赖 + 写 project 模块（先失败）**

`crates/core/Cargo.toml` 的 `[dependencies]` 加：
```toml
uuid = { version = "1", features = ["v4"] }
```

`crates/core/src/project.rs`：
```rust
//! project（项目实例）—— ~/.skm/projects/<project-id>.toml。installed_skills 是 apply 的
//! 唯一事实依据，locked_shas 是上次 apply 的基线快照（非版本锁）。
use crate::error::{atomic_write, Result, SkillkitError};
use crate::paths::Paths;
use serde::{Deserialize, Serialize};
use std::collections::BTreeMap;
use std::path::{Path, PathBuf};
use uuid::Uuid;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Project {
    /// 注册时生成的 8 hex 短码，冻结（rebind 不变）。
    pub id: String,
    /// 从 path basename，rebind 随新 path 调整。
    pub name: String,
    /// 项目实际位置，rebind 更新。
    pub path: String,
    #[serde(default)]
    pub agents: Vec<String>,
    #[serde(default)]
    pub applied_profiles: Vec<String>,
    #[serde(default)]
    pub installed_skills: Vec<String>,
    #[serde(default)]
    pub locked_shas: BTreeMap<String, String>,
}

/// 生成新 project-id：uuid v4 前 8 hex 大写（独立于 path，支持 rebind）。
pub fn new_id() -> String {
    Uuid::new_v4().simple().to_string()[..8].to_uppercase()
}

impl Project {
    /// 注册新项目：生成随机 id，name/path 取自传入路径。
    pub fn register(abs_path: PathBuf, agents: Vec<String>) -> Self {
        let name = abs_path
            .file_name()
            .map(|s| s.to_string_lossy().into_owned())
            .unwrap_or_else(|| "project".into());
        Self {
            id: new_id(),
            name,
            path: abs_path.to_string_lossy().into_owned(),
            agents,
            applied_profiles: vec![],
            installed_skills: vec![],
            locked_shas: BTreeMap::new(),
        }
    }

    pub fn load(paths: &Paths, id: &str) -> Result<Self> {
        let path = paths.projects_dir().join(format!("{id}.toml"));
        if !path.exists() {
            return Err(SkillkitError::ProjectNotFound { id: id.to_string() });
        }
        let content = std::fs::read_to_string(&path)?;
        Ok(toml::from_str(&content)?)
    }

    pub fn save(&self, paths: &Paths) -> Result<()> {
        let dir = paths.projects_dir();
        std::fs::create_dir_all(&dir)?;
        let path = dir.join(format!("{}.toml", self.id));
        atomic_write(&path, &toml::to_string_pretty(self)?)?;
        Ok(())
    }

    /// 重绑定：项目移动/改名后更新 path/name，id 不变。
    pub fn rebind(&mut self, new_path: &Path) {
        let abs = new_path.canonicalize().unwrap_or_else(|_| new_path.to_path_buf());
        self.path = abs.to_string_lossy().into_owned();
        if let Some(name) = abs.file_name() {
            self.name = name.to_string_lossy().into_owned();
        }
    }

    pub fn add_skill(&mut self, id: &str) -> Result<()> {
        if self.installed_skills.iter().any(|s| s == id) {
            return Err(SkillkitError::SkillAlreadyInstalled { id: id.to_string() });
        }
        self.installed_skills.push(id.to_string());
        Ok(())
    }

    pub fn remove_skill(&mut self, id: &str) -> Result<()> {
        let before = self.installed_skills.len();
        self.installed_skills.retain(|s| s != id);
        if self.installed_skills.len() == before {
            return Err(SkillkitError::SkillNotInstalled { id: id.to_string() });
        }
        Ok(())
    }

    pub fn apply_profile(&mut self, profile_name: &str, skill_ids: &[String]) {
        if !self.applied_profiles.iter().any(|p| p == profile_name) {
            self.applied_profiles.push(profile_name.to_string());
        }
        for id in skill_ids {
            if !self.installed_skills.iter().any(|s| s == id) {
                self.installed_skills.push(id.clone());
            }
        }
    }
}

pub fn list_ids(paths: &Paths) -> Result<Vec<String>> {
    let dir = paths.projects_dir();
    if !dir.exists() {
        return Ok(Vec::new());
    }
    let mut ids = Vec::new();
    for entry in std::fs::read_dir(&dir)? {
        let entry = entry?;
        let p = entry.path();
        if p.extension().and_then(|e| e.to_str()) == Some("toml") {
            if let Some(stem) = p.file_stem().and_then(|s| s.to_str()) {
                ids.push(stem.to_string());
            }
        }
    }
    ids.sort();
    Ok(ids)
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::paths::Paths;
    use tempfile::tempdir;

    #[test]
    fn new_id_is_short_hex_and_unique() {
        let id1 = new_id();
        let id2 = new_id();
        assert_eq!(id1.len(), 8, "8 hex 短码");
        assert!(id1.chars().all(|c| c.is_ascii_hexdigit()));
        assert_ne!(id1, id2, "每次随机生成应不同");
    }

    #[test]
    fn rebind_updates_path_name_keeps_id() {
        let mut proj = Project {
            id: "ABC12345".into(), name: "old".into(), path: "/tmp/old".into(),
            agents: vec![], applied_profiles: vec![], installed_skills: vec![],
            locked_shas: BTreeMap::new(),
        };
        proj.rebind(Path::new("/tmp/new-name"));
        assert_eq!(proj.id, "ABC12345", "重绑定 id 不变");
        assert_eq!(proj.name, "new-name");
        assert!(proj.path.contains("new-name"));
    }

    #[test]
    fn register_and_apply_profile_persists() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        let mut proj = Project::register(PathBuf::from("/tmp/demo"), vec!["claude-code".into()]);
        let id = proj.id.clone();
        proj.add_skill("dc/logseq").unwrap();
        proj.apply_profile("frontend", &["dc/dataviz".into(), "dc/logseq".into()]);
        assert_eq!(proj.installed_skills, vec!["dc/logseq", "dc/dataviz"]);
        proj.save(&paths).unwrap();

        let reloaded = Project::load(&paths, &id).unwrap();
        assert_eq!(reloaded.id, id);
        assert_eq!(reloaded.installed_skills.len(), 2);
    }

    #[test]
    fn load_missing_fails() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        assert!(matches!(
            Project::load(&paths, "nope"),
            Err(SkillkitError::ProjectNotFound { .. })
        ));
    }
}
```

- [ ] **Step 3: error.rs 加 ProjectNotFound**

`SkillkitError` 加：
```rust
    #[error("project 不存在：{id}（先 `skillkit project add <path>` 注册）")]
    ProjectNotFound { id: String },
```

- [ ] **Step 4: 跑测试确认红/绿**

Run: `cargo test -p skillkit-core project`
Expected: 若 Step 3 漏加 ProjectNotFound → 红；加全 → PASS（3 测试）。

- [ ] **Step 5: lib.rs 注册 + re-export**

加 `pub mod project;` 与 `pub use project::{list_ids as list_project_ids, Project};`。

Run: `cargo test -p skillkit-core project` → PASS。

- [ ] **Step 6: Commit**

```bash
git add crates/core/src/project.rs crates/core/src/paths.rs crates/core/src/error.rs crates/core/src/lib.rs
git commit -m "feat(core): project 注册表读写与 project-id 生成"
```

---

### Task 4: project CLI 注册（add / rebind / scan / list）

**Files:**
- Create: `crates/cli/src/commands/project.rs`（本 task 先实现 add/scan/list，后续 task 扩展）
- Modify: `crates/cli/src/commands/mod.rs`、`crates/cli/src/main.rs`

**Interfaces:**
- Consumes: `skillkit_core::{Project, list_project_ids, Config, paths::Paths}`（`Project::register` 关联函数注册，`Project::rebind` 重绑定）
- Produces: `project add / scan / list`

- [ ] **Step 1: 写 commands/project.rs（add/scan/list 部分）**

```rust
//! project 子命令：调 core 的 Project / apply。
use clap::{Args, Subcommand};
use skillkit_core::{config::Config, list_project_ids, paths::Paths, Project};
use std::path::PathBuf;

#[derive(Args)]
pub struct ProjectCmd {
    #[command(subcommand)]
    cmd: ProjectSub,
}

#[derive(Subcommand)]
enum ProjectSub {
    /// 注册项目（生成随机 8 hex project-id）
    Add {
        path: PathBuf,
        #[arg(long, value_delimiter = ',')]
        agents: Option<Vec<String>>,
    },
    /// 重绑定：项目移动/改名后更新 path/name，id 不变
    Rebind { id: String, path: PathBuf },
    /// 扫描目录发现项目（只列 path，不自动注册）
    Scan {
        dir: PathBuf,
        #[arg(long, default_value = "3")]
        depth: u32,
    },
    /// 列出已注册项目
    List,
    // 后续 task 加：ApplyProfile / AddSkill / RemoveSkill / Apply / Status
}

pub fn run(cmd: ProjectCmd) -> anyhow::Result<()> {
    let paths = Paths::production();
    match cmd.cmd {
        ProjectSub::Add { path, agents } => {
            let abs = path.canonicalize().unwrap_or_else(|_| path.clone());
            let cfg = Config::load(&paths)?;
            let agents = agents.unwrap_or_else(|| cfg.agents.iter().map(|a| a.name.clone()).collect());
            let proj = Project::register(abs, agents);
            let id = proj.id.clone();
            proj.save(&paths)?;
            println!("✓ 已注册项目 {id}");
        }
        ProjectSub::Rebind { id, path } => {
            let mut proj = Project::load(&paths, &id)?;
            proj.rebind(&path);
            proj.save(&paths)?;
            println!("✓ 已重绑定 {id} → {}", proj.path);
        }
        ProjectSub::Scan { dir, depth } => {
            let found = scan_projects(&dir, depth)?;
            if found.is_empty() {
                println!("（未发现项目，project scan 只识别含 .git 的目录）");
            }
            for p in found {
                println!("{}", p.display());
            }
        }
        ProjectSub::List => {
            for id in list_project_ids(&paths)? {
                let proj = Project::load(&paths, &id)?;
                println!("{:10} {} ({} skills)", id, proj.path, proj.installed_skills.len());
            }
        }
    }
    Ok(())
}

/// 扫描：找含 .git 的目录（depth 限制）。
fn scan_projects(dir: &std::path::Path, depth: u32) -> anyhow::Result<Vec<PathBuf>> {
    let mut found = Vec::new();
    if dir.join(".git").exists() {
        found.push(dir.to_path_buf());
    }
    if depth > 0 {
        if let Ok(entries) = std::fs::read_dir(dir) {
            for entry in entries.flatten() {
                let p = entry.path();
                if p.is_dir() && !p.starts_with(dir.join(".git")) {
                    found.extend(scan_projects(&p, depth - 1)?);
                }
            }
        }
    }
    Ok(found)
}
```

- [ ] **Step 2: 接线 main.rs + mod.rs**

`commands/mod.rs` 加 `pub mod project;`。`main.rs` 的 `Cmd` 加 `Project(ProjectCmd)` 分支，顶部 `use commands::project::ProjectCmd;`。

- [ ] **Step 3: 验证 add/rebind/list/scan**

Run:
```bash
make build && TESTHOME=$(mktemp -d) && DEMO=$(mktemp -d) && git init -q $DEMO && \
ID=$(HOME=$TESTHOME ./target/debug/skillkit project add $DEMO | awk '{print $4}') && \
HOME=$TESTHOME ./target/debug/skillkit project list && \
NEWDEMO=$(mktemp -d) && git init -q $NEWDEMO && \
HOME=$TESTHOME ./target/debug/skillkit project rebind $ID $NEWDEMO && \
HOME=$TESTHOME ./target/debug/skillkit project list && \
HOME=$TESTHOME ./target/debug/skillkit project scan "$(dirname $DEMO)" --depth 1 && \
rm -rf $TESTHOME $DEMO $NEWDEMO
```
Expected: add 打印随机 8 hex project-id；rebind 后 list 的**同一 id** 指向 NEWDEMO（path 更新、id 不变）；scan 列出发现的目录 path。

- [ ] **Step 4: Commit**

```bash
git add crates/cli/src/
git commit -m "feat(cli): project 注册命令（add/rebind/scan/list）"
```

---

### Task 5: project CLI 声明编辑（apply-profile / add-skill / remove-skill）

**Files:**
- Modify: `crates/cli/src/commands/project.rs`

**Interfaces:**
- Consumes: `Project`, `Profile`
- Produces: `project apply-profile / add-skill / remove-skill`

- [ ] **Step 1: ProjectCmd 的 ProjectSub 加三个变体**

在 `enum ProjectSub` 加：
```rust
    /// 把 profile 的 skill 批量灌入 installed_skills
    ApplyProfile { project: String, profile: String },
    /// 精确加单个 skill
    AddSkill { project: String, id: String },
    /// 精确删单个 skill
    RemoveSkill { project: String, id: String },
```

`run` 的 match 加：
```rust
        ProjectSub::ApplyProfile { project, profile } => {
            let mut proj = Project::load(&paths, &project)?;
            let p = skillkit_core::Profile::load(&paths, &profile)?;
            proj.apply_profile(&profile, &p.skills);
            proj.save(&paths)?;
            println!("✓ {project} 已应用 profile {profile}（{} skills）", proj.installed_skills.len());
        }
        ProjectSub::AddSkill { project, id } => {
            let mut proj = Project::load(&paths, &project)?;
            proj.add_skill(&id)?;
            proj.save(&paths)?;
            println!("✓ {project} 已加 {id}");
        }
        ProjectSub::RemoveSkill { project, id } => {
            let mut proj = Project::load(&paths, &project)?;
            proj.remove_skill(&id)?;
            proj.save(&paths)?;
            println!("✓ {project} 已移除 {id}");
        }
```

- [ ] **Step 2: 验证声明编辑流程**

Run: `make build && TESTHOME=$(mktemp -d) && DEMO=$(mktemp -d) && git init -q $DEMO && PID=$(HOME=$TESTHOME ./target/debug/skillkit project add $DEMO | awk '{print $3}') && HOME=$TESTHOME ./target/debug/skillkit project add-skill $PID dc/logseq && HOME=$TESTHOME ./target/debug/skillkit project list && rm -rf $TESTHOME $DEMO`
Expected: add-skill 成功，list 显示 1 skill。

- [ ] **Step 3: Commit**

```bash
git add crates/cli/src/commands/project.rs
git commit -m "feat(cli): project 声明编辑（apply-profile/add-skill/remove-skill）"
```

---

### Task 6: apply diff 逻辑（core，纯逻辑）

**Files:**
- Create: `crates/core/src/apply.rs`（本 task 先实现 diff 计算）
- Modify: `crates/core/src/lib.rs`

**Interfaces:**
- Consumes: `Project`, `Registry`, `Scope`
- Produces: `ApplyDiff { expected, missing, extra, conflicts }`、`compute_diff(...) -> ApplyDiff`

**设计决策（spec §11 --json 结构定档）：** `ApplyDiff` 字段对齐 `project status --json` 的 `{expected, missing, extra, conflicts}`。expected = installed_skills 中应落地的 local skill（global 不 per-project 落地，不进 expected）；missing = 该有未落地；extra = 现状有但声明没有（skillkit 管的 local）；conflicts = locked_shas 与 registry.commit_sha 不一致。

- [ ] **Step 1: 写 apply diff 测试（先失败）**

`crates/core/src/apply.rs`：
```rust
//! apply：让项目 <agent>/skills/ 下 skillkit 管的 local skill 与 installed_skills 一致。
//! 本模块含 diff 计算（纯逻辑，status 与 apply 共用）+ 落地执行（Task 7-8）。
use crate::error::Result;
use crate::project::Project;
use crate::registry::{Registry, Scope};
use serde::{Deserialize, Serialize};

/// 一个 skill 在某 agent 下的落地目标（Task 7 落地用）。
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct LocalTarget {
    pub skill_id: String,
    pub agent: String,
    pub canonical_path: String,
    pub commit_sha: String,
}

/// apply 内部 diff：expected（应落地的 local target）+ conflicts（sha 漂移的 skill）。
/// missing/extra 不在此（需结合现状扫描，由 build_status 算到 StatusView）。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ApplyDiff {
    pub expected: Vec<LocalTarget>,
    pub conflicts: Vec<String>,
}

/// 计算 diff：expected = installed_skills 中 local scope 的 skill × agents；
/// conflicts = locked_shas 与 registry.commit_sha 不一致。
/// missing/extra 需要现状扫描，在 Task 7 落地时结合 expected 与现状得出。
pub fn compute_diff(project: &Project, registry: &Registry) -> Result<ApplyDiff> {
    let mut expected = Vec::new();
    let mut conflicts = Vec::new();
    for id in &project.installed_skills {
        let meta = match registry.get(id) {
            Ok(m) => m,
            Err(_) => {
                // 未安装：apply 时报错引导 install；diff 阶段跳过（status 也会标 missing）
                continue;
            }
        };
        if meta.scope != Scope::Local {
            continue; // global 不 per-project 落地
        }
        let sha = meta.commit_sha.clone().unwrap_or_default();
        // 冲突检测：locked_shas 基线 vs registry 当前
        if let Some(locked) = project.locked_shas.get(id) {
            if locked != &sha {
                conflicts.push(id.clone());
            }
        }
        let canonical = meta.canonical_path.clone();
        for agent in &project.agents {
            expected.push(LocalTarget {
                skill_id: id.clone(),
                agent: agent.clone(),
                canonical_path: canonical.clone(),
                commit_sha: sha.clone(),
            });
        }
    }
    Ok(ApplyDiff { expected, conflicts })
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::project::Project;
    use crate::registry::{Scope, SkillMeta};
    use std::collections::BTreeMap;

    fn meta(id: &str, scope: Scope, sha: &str) -> SkillMeta {
        SkillMeta {
            id: id.into(), name: id.split('/').last().unwrap_or(id).into(),
            source: id.split('/').next().unwrap_or("").into(), scope,
            version: None, commit_sha: Some(sha.into()),
            installed_at: "2026-07-29T00:00:00Z".into(),
            canonical_path: format!("/canon/{}", id.split('/').last().unwrap_or(id)),
        }
    }

    fn proj(skills: &[&str], locked: &[(&str, &str)]) -> Project {
        Project {
            name: "p".into(), path: "/tmp/p".into(),
            agents: vec!["claude-code".into()], applied_profiles: vec![],
            installed_skills: skills.iter().map(|s| s.to_string()).collect(),
            locked_shas: locked.iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }

    #[test]
    fn diff_expected_only_local_global_skipped() {
        let mut reg = Registry::default();
        reg.upsert(meta("dc/logseq", Scope::Local, "sha1"));
        reg.upsert(meta("dc/global", Scope::Global, "sha2"));
        let project = proj(&["dc/logseq", "dc/global"], &[]);
        let diff = compute_diff(&project, &reg).unwrap();
        assert_eq!(diff.expected.len(), 1, "global 不 per-project 落地");
        assert_eq!(diff.expected[0].skill_id, "dc/logseq");
        assert!(diff.conflicts.is_empty());
    }

    #[test]
    fn diff_conflicts_when_sha_drifted() {
        let mut reg = Registry::default();
        reg.upsert(meta("dc/logseq", Scope::Local, "new"));
        let project = proj(&["dc/logseq"], &[("dc/logseq", "old")]);
        let diff = compute_diff(&project, &reg).unwrap();
        assert_eq!(diff.conflicts, vec!["dc/logseq"]);
    }

    #[test]
    fn diff_skips_uninstalled() {
        let reg = Registry::default();
        let project = proj(&["dc/missing"], &[]);
        let diff = compute_diff(&project, &reg).unwrap();
        assert!(diff.expected.is_empty());
    }
}
```

- [ ] **Step 2: 跑测试确认红（compute_diff 未注册到 lib → 编译失败，或函数已写在同文件则绿；红绿点在 lib 未 pub mod apply）**

Run: `cargo test -p skillkit-core apply`
Expected: 若 lib 未加 `pub mod apply;` → 模块不编译，测试不跑（红）；加后 PASS（3 测试）。

- [ ] **Step 3: lib.rs 注册**

加 `pub mod apply;` 与 `pub use apply::{compute_diff, ApplyDiff, LocalTarget};`。

Run: `cargo test -p skillkit-core apply` → PASS。

- [ ] **Step 4: Commit**

```bash
git add crates/core/src/apply.rs crates/core/src/lib.rs
git commit -m "feat(core): apply diff 计算（expected/conflicts，global 不落地）"
```

---

### Task 7: apply local 落地 + .git/info/exclude（core）

**Files:**
- Modify: `crates/core/src/apply.rs`
- Modify: `crates/core/src/config.rs`（加按 name 查 agent 能力的辅助）

**Interfaces:**
- Consumes: `Config`, `Agent`, `Paths`
- Produces: `apply_local(paths, project, diff) -> Result<ApplyReport>`、exclude 维护

**落地规则（spec §10.2/§13）：**
- Claude（supports_symlink=true）：`<project>/.claude/skills/<skill>` → canonical 的 symlink（local 用 ~/.skm/skills/<skill>）。
- Cursor（supports_symlink=false）：copy canonical → `<project>/.cursor/skills/<skill>/`，并在副本写 `.skillkit-sha`（commit_sha），下次 apply 比对过期则重 copy。
- local 与 shared 同名：shared（git 真实文件）优先，local 跳过 + 记 extra/警告。
- local 已被 git 追踪：`.git/info/exclude` 对已追踪无效，记 conflicts/警告，提示 `git rm --cached`。
- exclude 维护：重写 `<project>/.git/info/exclude` 的 skillkit 段（标记区间），写入当前 local 落地清单（每行 `<agent>/skills/<skill>`）。

- [ ] **Step 1: config.rs 加 find_agent**

`impl Config` 加：
```rust
    pub fn find_agent(&self, name: &str) -> Option<&Agent> {
        self.agents.iter().find(|a| a.name == name)
    }
```

- [ ] **Step 2: 写 apply 落地测试（先失败）**

在 `apply.rs` 的 `#[cfg(test)] mod tests` 前加落地实现 + 报告类型，并追加测试：

```rust
// 追加到 apply.rs 顶部 use 区（与 Task 6 合并去重；Task 6 已 use crate::error::Result，
// 此处只加 Task 7 新增项）：
use crate::config::Config;
use crate::error::SkillkitError;
use crate::paths::Paths;
use std::path::{Path, PathBuf};

/// 落地动作记录（status/apply 输出用）。
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ApplyReport {
    pub created: Vec<String>,
    pub removed: Vec<String>,
    pub recopied: Vec<String>,   // Cursor copy 过期重 copy
    pub warnings: Vec<String>,   // shared 同名 / 已追踪 / dangling
}

const EXCLUDE_BEGIN: &str = "# >>> skillkit managed >>>";
const EXCLUDE_END: &str = "# <<< skillkit managed <<<";

/// 对一个 local target 落地（按 agent 能力 symlink 或 copy）。返回是否新建/重 copy。
fn land_one(project_root: &Path, target: &LocalTarget, supports_symlink: bool) -> Result<(bool, bool)> {
    let skill = target.skill_id.split('/').last().unwrap_or(&target.skill_id);
    let agent_dir = project_root.join(format!(".{}/skills", target.agent));
    std::fs::create_dir_all(&agent_dir)?;
    let dest = agent_dir.join(skill);
    let canonical = Path::new(&target.canonical_path);

    // shared 同名（真实目录/文件，非 skillkit symlink）：跳过 + 警告
    if dest.exists() && !dest.is_symlink() && std::fs::metadata(&dest).map(|m| m.is_dir()).unwrap_or(false) {
        // 可能是 shared（git 真实）或上次 copy。copy 模式下要判断是否过期（.skillkit-sha）
        if !supports_symlink {
            let sha_file = dest.join(".skillkit-sha");
            let current = std::fs::read_to_string(&sha_file).unwrap_or_default();
            if current == target.commit_sha {
                return Ok((false, false)); // 副本未过期，幂等跳过
            }
            // 过期：删旧重 copy
            std::fs::remove_dir_all(&dest)?;
            copy_dir_all(canonical, &dest)?;
            std::fs::write(sha_file, &target.commit_sha)?;
            return Ok((false, true));
        }
        // symlink 模式下遇到真实目录：疑似 shared，跳过 + 警告（上层处理）
        return Err(SkillkitError::Git { message: format!("{} 已存在且非 symlink，疑似 shared，跳过 local 落地", dest.display()) });
    }

    if supports_symlink {
        if let Ok(existing) = std::fs::read_link(&dest) {
            if existing.as_path() == canonical {
                return Ok((false, false)); // 幂等
            }
            std::fs::remove_file(&dest)?;
        }
        #[cfg(unix)]
        std::os::unix::fs::symlink(canonical, &dest)
            .map_err(|e| SkillkitError::Git { message: format!("symlink 失败：{e}") })?;
        Ok((true, false))
    } else {
        copy_dir_all(canonical, &dest)?;
        std::fs::write(dest.join(".skillkit-sha"), &target.commit_sha)?;
        Ok((true, false))
    }
}

fn copy_dir_all(src: &Path, dst: &Path) -> Result<()> {
    let status = std::process::Command::new("cp").arg("-R").arg(src).arg(dst).status()
        .map_err(|e| SkillkitError::Git { message: e.to_string() })?;
    if !status.success() {
        return Err(SkillkitError::Git { message: format!("复制失败：{}", src.display()) });
    }
    Ok(())
}

/// 重写 <project>/.git/info/exclude 的 skillkit 段，列入当前 local 落地清单。
pub fn write_exclude(project_root: &Path, targets: &[LocalTarget]) -> Result<()> {
    let exclude = project_root.join(".git/info/exclude");
    if let Some(parent) = exclude.parent() {
        std::fs::create_dir_all(parent)?;
    }
    let mut lines: Vec<String> = Vec::new();
    // 保留非 skillkit 段
    if let Ok(content) = std::fs::read_to_string(&exclude) {
        let mut in_block = false;
        for line in content.lines() {
            if line == EXCLUDE_BEGIN { in_block = true; continue; }
            if line == EXCLUDE_END { in_block = false; continue; }
            if !in_block { lines.push(line.into()); }
        }
    }
    // 追加 skillkit 段
    lines.push(EXCLUDE_BEGIN.into());
    for t in targets {
        let skill = t.skill_id.split('/').last().unwrap_or(&t.skill_id);
        lines.push(format!(".{}/skills/{}", t.agent, skill));
    }
    lines.push(EXCLUDE_END.into());
    crate::error::atomic_write(&exclude, &lines.join("\n"))?;
    Ok(())
}
```

测试追加（在 mod tests 内）：
```rust
    use crate::paths::Paths;
    use tempfile::tempdir;

    fn global_paths() -> (Paths, PathBuf) {
        let tmp = tempdir().unwrap();
        (Paths::new(tmp.path().to_path_buf()), tmp.path().to_path_buf())
    }

    #[test]
    fn land_symlink_idempotent_and_exclude() {
        let (paths, home) = global_paths();
        let project_root = home.join("proj");
        std::fs::create_dir_all(project_root.join(".git/info")).unwrap();
        // canonical
        let canon = home.join(".skm/skills/logseq");
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();
        let target = LocalTarget {
            skill_id: "dc/logseq".into(), agent: "claude-code".into(),
            canonical_path: canon.to_string_lossy().into_owned(), commit_sha: "sha1".into(),
        };
        let (created, _) = land_one(&project_root, &target, true).unwrap();
        assert!(created);
        let link = project_root.join(".claude/skills/logseq");
        assert!(link.is_symlink());
        // 幂等
        let (created2, _) = land_one(&project_root, &target, true).unwrap();
        assert!(!created2);
        // exclude
        write_exclude(&project_root, &[target]).unwrap();
        let excl = std::fs::read_to_string(project_root.join(".git/info/exclude")).unwrap();
        assert!(excl.contains(".claude/skills/logseq"));
    }

    #[test]
    fn land_copy_with_sha_and_recopy_on_drift() {
        let (paths, home) = global_paths();
        let project_root = home.join("proj");
        std::fs::create_dir_all(&project_root).unwrap();
        let canon = home.join(".skm/skills/foo");
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "v1").unwrap();
        let t1 = LocalTarget {
            skill_id: "dc/foo".into(), agent: "cursor".into(),
            canonical_path: canon.to_string_lossy().into_owned(), commit_sha: "sha1".into(),
        };
        land_one(&project_root, &t1, false).unwrap();
        let dest = project_root.join(".cursor/skills/foo");
        assert!(dest.join("SKILL.md").exists());
        assert_eq!(std::fs::read_to_string(dest.join(".skillkit-sha")).unwrap(), "sha1");
        // canonical 升级 + drift
        std::fs::write(canon.join("SKILL.md"), "v2").unwrap();
        let t2 = LocalTarget { commit_sha: "sha2".into(), ..t1 };
        let (_, recopied) = land_one(&project_root, &t2, false).unwrap();
        assert!(recopied, "sha 漂移应触发重 copy");
    }
```

- [ ] **Step 3: 跑测试确认红/绿**

Run: `cargo test -p skillkit-core apply`
Expected: Task 6 的 3 测试 + 本 task 2 测试 = 5 PASS（land_one/write_exclude 同文件实现，lib 已 pub mod apply）。

- [ ] **Step 4: Commit**

```bash
git add crates/core/src/apply.rs crates/core/src/config.rs
git commit -m "feat(core): apply local 落地（symlink/copy + sha 标记 + exclude 维护）"
```

---

### Task 8: apply 编排（core 主流程）

**Files:**
- Modify: `crates/core/src/apply.rs`

**Interfaces:**
- Consumes: `compute_diff`, `land_one`, `write_exclude`, `Registry`, `Config`, `ensure_global_claude`
- Produces: `run_apply(paths, project, frozen) -> Result<ApplyReport>`

**编排（spec §10.2）：**
1. load registry + config。
2. `compute_diff` 得 expected + conflicts。
3. 未安装的 installed_skill → 报错引导 install。
4. conflicts：`--frozen` 报错退出；否则以 canonical 为准（落地后更新 locked_shas）。
5. global skill（installed_skills 中 scope=global）：ensure canonical + Claude symlink 在位（复用 `ensure_global_claude`，不 per-project 落地）。
6. local：对 expected 每个 target 调 `land_one`（按 config.find_agent(agent).supports_symlink）；收集 created/recopied/warnings。
7. extra：扫描现状 skillkit-local 落地点不在 expected 的，删除（symlink 删链接、copy 删目录；shared 不碰）。
8. `write_exclude`。
9. 更新 locked_shas = 各 skill 当前 commit_sha，save project。

- [ ] **Step 1: 写 run_apply 测试（先失败）**

`apply.rs` 加 `run_apply`，测试追加：
```rust
/// 扫描 <project>/<agent>/skills/ 下 skillkit 管的 local 落地点。
/// skillkit-local 判定：symlink 指向 ~/.skm/skills/，或目录含 .skillkit-sha。
fn scan_local_landed(project_root: &Path, agent: &str, skm_skills: &Path) -> Result<Vec<String>> {
    let dir = project_root.join(format!(".{agent}/skills"));
    if !dir.exists() { return Ok(vec![]); }
    let mut found = Vec::new();
    for entry in std::fs::read_dir(&dir)? {
        let entry = entry?;
        let p = entry.path();
        let name = entry.file_name().to_string_lossy().into_owned();
        if p.is_symlink() {
            if let Ok(t) = std::fs::read_link(&p) {
                if t.starts_with(skm_skills) { found.push(name); }
            }
        } else if p.is_dir() && p.join(".skillkit-sha").exists() {
            found.push(name);
        }
    }
    Ok(found)
}

/// apply 主流程。
pub fn run_apply(paths: &Paths, project: &mut Project, frozen: bool) -> Result<ApplyReport> {
    let registry = Registry::load(paths)?;
    let config = Config::load(paths)?;
    let diff = compute_diff(project, &registry)?;
    let project_root = Path::new(&project.path);

    // 未安装检查
    for id in &project.installed_skills {
        if registry.get(id).is_err() {
            return Err(SkillkitError::SkillNotInstalled { id: id.clone() });
        }
    }
    // 冲突
    if frozen && !diff.conflicts.is_empty() {
        return Err(SkillkitError::Git { message: format!("--frozen：版本冲突 {}", diff.conflicts.join(", ")) });
    }

    let mut report = ApplyReport::default();

    // global skill：ensure canonical + Claude symlink 在位
    for id in &project.installed_skills {
        let meta = registry.get(id)?;
        if meta.scope == Scope::Global {
            crate::symlink::ensure_global_claude(paths, meta)?;
        }
    }

    // local 落地
    let skm_skills = paths.skm_skills_dir();
    for target in &diff.expected {
        let supports_symlink = config
            .find_agent(&target.agent)
            .map(|a| a.supports_symlink)
            .unwrap_or(false);
        match land_one(project_root, target, supports_symlink) {
            Ok((created, recopied)) => {
                if created { report.created.push(format!("{}/{}", target.agent, target.skill_id)); }
                if recopied { report.recopied.push(format!("{}/{}", target.agent, target.skill_id)); }
            }
            Err(e) => report.warnings.push(format!("{}：{e}", target.skill_id)),
        }
    }

    // extra：扫描现状 skillkit-local，删不在 expected 的
    let expected_names: std::collections::HashSet<String> = diff
        .expected
        .iter()
        .map(|t| format!("{}/{}", t.agent, t.skill_id.split('/').last().unwrap_or(&t.skill_id)))
        .collect();
    for agent in &project.agents {
        for name in scan_local_landed(project_root, agent, &skm_skills)? {
            let key = format!("{agent}/{name}");
            if !expected_names.contains(&key) {
                let p = project_root.join(format!(".{agent}/skills/{name}"));
                let _ = std::fs::remove_file(&p).or_else(|_| std::fs::remove_dir_all(&p));
                report.removed.push(key);
            }
        }
    }

    write_exclude(project_root, &diff.expected)?;

    // 更新 locked_shas 为当前 canonical sha
    for target in &diff.expected {
        project.locked_shas.insert(target.skill_id.clone(), target.commit_sha.clone());
    }
    Ok(report)
}
```

测试追加：
```rust
    use crate::registry::Registry;

    fn install_local_bare(paths: &Paths, id: &str, sha: &str) {
        // 在 skm_skills/<skill> 造 canonical（模拟 install local 的产物）
        let skill = id.split('/').last().unwrap_or(id);
        let canon = paths.skm_skills_dir().join(skill);
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();
        let mut reg = Registry::load(paths).unwrap();
        reg.upsert(crate::registry::SkillMeta {
            id: id.into(), name: skill.into(), source: id.split('/').next().unwrap_or("").into(),
            scope: Scope::Local, version: None, commit_sha: Some(sha.into()),
            installed_at: "2026-07-29T00:00:00Z".into(),
            canonical_path: canon.to_string_lossy().into_owned(),
        });
        reg.save(paths).unwrap();
    }

    #[test]
    fn run_apply_lands_local_and_locks_sha() {
        let (paths, home) = global_paths();
        install_local_bare(&paths, "dc/logseq", "sha1");
        let project_root = home.join("proj");
        std::fs::create_dir_all(project_root.join(".git/info")).unwrap();
        let mut proj = Project {
            name: "p".into(), path: project_root.to_string_lossy().into_owned(),
            agents: vec!["claude-code".into()], applied_profiles: vec![],
            installed_skills: vec!["dc/logseq".into()], locked_shas: BTreeMap::new(),
        };
        // config 默认 claude-code supports_symlink=true
        let report = run_apply(&paths, &mut proj, false).unwrap();
        assert_eq!(report.created, vec!["claude-code/logseq"]);
        assert!(project_root.join(".claude/skills/logseq").is_symlink());
        assert_eq!(proj.locked_shas.get("dc/logseq").unwrap(), "sha1");

        // 幂等：再 apply 零 created
        let report2 = run_apply(&paths, &mut proj, false).unwrap();
        assert!(report2.created.is_empty());
    }

    #[test]
    fn run_apply_removes_extra() {
        let (paths, home) = global_paths();
        install_local_bare(&paths, "dc/keep", "sha1");
        install_local_bare(&paths, "dc/gone", "sha2");
        let project_root = home.join("proj");
        std::fs::create_dir_all(project_root.join(".git/info")).unwrap();
        let mut proj = Project {
            name: "p".into(), path: project_root.to_string_lossy().into_owned(),
            agents: vec!["claude-code".into()], applied_profiles: vec![],
            installed_skills: vec!["dc/keep".into(), "dc/gone".into()], locked_shas: BTreeMap::new(),
        };
        run_apply(&paths, &mut proj, false).unwrap();
        assert!(project_root.join(".claude/skills/gone").is_symlink());
        // 移除 gone 再 apply
        proj.remove_skill("dc/gone").unwrap();
        let report = run_apply(&paths, &mut proj, false).unwrap();
        assert!(report.removed.iter().any(|r| r.contains("gone")));
        assert!(!project_root.join(".claude/skills/gone").exists());
    }

    #[test]
    fn run_apply_frozen_conflict_errors() {
        let (paths, home) = global_paths();
        install_local_bare(&paths, "dc/logseq", "new");
        let project_root = home.join("proj");
        std::fs::create_dir_all(project_root.join(".git/info")).unwrap();
        let mut proj = Project {
            name: "p".into(), path: project_root.to_string_lossy().into_owned(),
            agents: vec!["claude-code".into()], applied_profiles: vec![],
            installed_skills: vec!["dc/logseq".into()],
            locked_shas: [("dc/logseq".to_string(), "old".into())].into_iter().collect(),
        };
        let err = run_apply(&paths, &mut proj, true).unwrap_err();
        assert!(matches!(err, SkillkitError::Git { .. }));
    }
```

- [ ] **Step 2: 跑测试确认绿**

Run: `cargo test -p skillkit-core apply`
Expected: 5（Task 6-7）+ 3（本 task）= 8 PASS。

- [ ] **Step 3: Commit**

```bash
git add crates/core/src/apply.rs
git commit -m "feat(core): apply 编排（落地+extra 清理+locked_shas 更新+frozen）"
```

---

### Task 9: project status + apply CLI

**Files:**
- Modify: `crates/cli/src/commands/project.rs`

**Interfaces:**
- Consumes: `run_apply`, `compute_diff`, `Registry`, `ApplyReport`
- Produces: `project apply / status`（含 `--json`、`--frozen`）

- [ ] **Step 1: ProjectSub 加 Apply / Status 变体**

`enum ProjectSub` 加：
```rust
    /// 幂等落地
    Apply {
        project: String,
        #[arg(long)]
        frozen: bool,
        #[arg(long)]
        json: bool,
    },
    /// 输出 diff（该有/缺/多/冲突）
    Status {
        project: String,
        #[arg(long)]
        json: bool,
    },
```

`run` 的 match 加：
```rust
        ProjectSub::Apply { project, frozen, json } => {
            let mut proj = Project::load(&paths, &project)?;
            let report = skillkit_core::apply::run_apply(&paths, &mut proj, frozen)?;
            proj.save(&paths)?;
            if json {
                println!("{}", serde_json::to_string_pretty(&report)?);
            } else {
                println!("✓ applied：{} created, {} removed, {} recopied, {} warnings",
                    report.created.len(), report.removed.len(), report.recopied.len(), report.warnings.len());
                for w in &report.warnings { println!("  ⚠ {w}"); }
            }
        }
        ProjectSub::Status { project, json } => {
            let proj = Project::load(&paths, &project)?;
            let reg = skillkit_core::Registry::load(&paths)?;
            let diff = skillkit_core::apply::compute_diff(&proj, &reg)?;
            // 扫描现状补 missing/extra
            let status = skillkit_core::apply::build_status(&paths, &proj, &diff)?;
            if json {
                println!("{}", serde_json::to_string_pretty(&status)?);
            } else {
                println!("expected:  {}", status.expected.join(", "));
                println!("missing:   {}", status.missing.join(", "));
                println!("extra:     {}", status.extra.join(", "));
                println!("conflicts: {}", status.conflicts.join(", "));
            }
        }
```

- [ ] **Step 2: apply.rs 加 build_status（status 用，扫描现状算 missing/extra）**

`apply.rs` 加：
```rust
/// status 输出：结合 diff.expected 与现状扫描，给具体 id 清单（供 agent 决策）。
#[derive(Debug, Clone, Serialize)]
pub struct StatusView {
    pub expected: Vec<String>,   // 应落地的 local skill（agent/skill）
    pub missing: Vec<String>,    // 该有未落地
    pub extra: Vec<String>,      // 现状多出（skillkit 管的 local）
    pub conflicts: Vec<String>,  // sha 漂移的 skill_id
}

pub fn build_status(paths: &Paths, project: &Project, diff: &ApplyDiff) -> Result<StatusView> {
    let project_root = Path::new(&project.path);
    let skm_skills = paths.skm_skills_dir();
    let mut expected = Vec::new();
    let mut missing = Vec::new();
    for t in &diff.expected {
        let skill = t.skill_id.split('/').last().unwrap_or(&t.skill_id);
        let key = format!("{}/{}", t.agent, skill);
        expected.push(key.clone());
        let dest = project_root.join(format!(".{}/skills/{}", t.agent, skill));
        if !dest.exists() && !dest.is_symlink() {
            missing.push(key);
        }
    }
    let mut extra = Vec::new();
    for agent in &project.agents {
        for name in scan_local_landed(project_root, agent, &skm_skills)? {
            let key = format!("{agent}/{name}");
            if !expected.contains(&key) {
                extra.push(key);
            }
        }
    }
    Ok(StatusView {
        expected,
        missing,
        extra,
        conflicts: diff.conflicts.clone(),
    })
}
```

注：status --json 给 id 清单（expected/missing/extra/conflicts 具体到 agent/skill），agent 据此 install 缺的、remove 多的、决策冲突，与 apply 形成「感知-执行」闭环（spec §11）。

- [ ] **Step 3: 验证 status/apply CLI**

Run: `make build && TESTHOME=$(mktemp -d) && DEMO=$(mktemp -d) && git init -q $DEMO && SK=$(mktemp -d) && ...`
（完整脚本较长，核心断言：apply 后 status missing=0；remove-skill 后 apply 清理 extra。详见 Task 10 e2e。）

- [ ] **Step 4: Commit**

```bash
git add crates/cli/src/commands/project.rs crates/core/src/apply.rs
git commit -m "feat(cli): project apply/status 命令（含 --json、--frozen）"
```

---

### Task 10: --json schema 锁定 + now_iso 真实时间 + install 表面对齐 + m1_e2e

**Files:**
- Modify: `crates/core/Cargo.toml`（加 chrono）
- Modify: `crates/core/src/install.rs`（now_iso 用 chrono）
- Modify: `crates/cli/src/commands/install.rs`（install 命令 help 注明 spec 表面）
- Create: `crates/core/tests/m1_e2e.rs`
- Create: `crates/core/src/json_schema_tests.rs`（或集成进 m1_e2e）

- [ ] **Step 1: 加 chrono，now_iso 真实时间**

`crates/core/Cargo.toml` 的 `[dependencies]` 加 `chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }`。

`install.rs` 的 `now_iso` 改：
```rust
fn now_iso() -> String {
    chrono::Utc::now().to_rfc3339()
}
```

Run: `cargo test -p skillkit-core` → 既有测试不受影响（installed_at 不再固定，但无测试断言其具体值）。

- [ ] **Step 2: install 命令表面对齐（文档层）**

`cli/src/commands/install.rs` 的 `InstallSub::Add` doc 注明：
```rust
    /// 安装 skill：skillkit install add <source> <skill> [--scope global|local]
    /// （spec §11 的 `install <id>` 简写：id = <source>/<skill>，本实现用 add 子命令显式拆分两个参数）
```

并在 `docs/sessions` 交接记录 §5.2 标注已对齐（install add 即 spec install <id> 的显式形式，不改命令）。

- [ ] **Step 3: 写 m1_e2e（闭环验收）**

`crates/core/tests/m1_e2e.rs`：
```rust
//! M1 端到端：install local → profile → project add → apply-profile → apply 落地 → status → 幂等 → extra 清理。
use skillkit_core::{
    apply::{build_status, run_apply}, config::Config, paths::Paths,
    profile::Profile, project::Project, registry::Registry, Scope,
};
use tempfile::tempdir;

fn install_local_bare(paths: &Paths, id: &str) {
    let skill = id.split('/').last().unwrap_or(id);
    let canon = paths.skm_skills_dir().join(skill);
    std::fs::create_dir_all(&canon).unwrap();
    std::fs::write(canon.join("SKILL.md"), "# local\n").unwrap();
    let mut reg = Registry::load(paths).unwrap();
    reg.upsert(skillkit_core::registry::SkillMeta {
        id: id.into(), name: skill.into(), source: id.split('/').next().unwrap_or("").into(),
        scope: Scope::Local, version: None, commit_sha: Some("sha1".into()),
        installed_at: "2026-07-29T00:00:00Z".into(),
        canonical_path: canon.to_string_lossy().into_owned(),
    });
    reg.save(paths).unwrap();
}

#[test]
fn m1_full_flow_apply_and_status() {
    let tmp = tempdir().unwrap();
    let paths = Paths::new(tmp.path().to_path_buf());
    let project_root = tmp.path().join("proj");
    std::fs::create_dir_all(project_root.join(".git/info")).unwrap();

    install_local_bare(&paths, "dc/logseq");
    install_local_bare(&paths, "dc/dataviz");

    // profile
    let mut profile = Profile { name: "frontend".into(), description: String::new(), skills: vec![] };
    profile.add_skill("dc/logseq").unwrap();
    profile.add_skill("dc/dataviz").unwrap();
    profile.save(&paths).unwrap();

    // project
    let mut proj = Project {
        name: "proj".into(), path: project_root.to_string_lossy().into_owned(),
        agents: vec!["claude-code".into()], applied_profiles: vec![],
        installed_skills: vec![], locked_shas: Default::default(),
    };
    proj.apply_profile("frontend", &profile.skills);
    proj.save(&paths).unwrap();

    // status（apply 前 missing=2）
    let reg = Registry::load(&paths).unwrap();
    let diff = skillkit_core::apply::compute_diff(&proj, &reg).unwrap();
    let st = build_status(&paths, &proj, &diff).unwrap();
    assert_eq!(st.missing.len(), 2);

    // apply
    let report = run_apply(&paths, &mut proj, false).unwrap();
    assert_eq!(report.created.len(), 2);
    assert!(project_root.join(".claude/skills/logseq").is_symlink());
    assert!(project_root.join(".claude/skills/dataviz").is_symlink());

    // 幂等
    let r2 = run_apply(&paths, &mut proj, false).unwrap();
    assert!(r2.created.is_empty());

    // extra 清理
    proj.remove_skill("dc/dataviz").unwrap();
    let r3 = run_apply(&paths, &mut proj, false).unwrap();
    assert!(r3.removed.iter().any(|r| r.contains("dataviz")));
    assert!(!project_root.join(".claude/skills/dataviz").exists());

    // exclude 含 logseq
    let excl = std::fs::read_to_string(project_root.join(".git/info/exclude")).unwrap();
    assert!(excl.contains(".claude/skills/logseq"));
}

#[test]
fn m1_global_skill_not_per_project() {
    let tmp = tempdir().unwrap();
    let paths = Paths::new(tmp.path().to_path_buf());
    let project_root = tmp.path().join("proj");
    std::fs::create_dir_all(project_root.join(".git/info")).unwrap();
    // global canonical
    let canon = paths.agents_skills_dir().join("gskill");
    std::fs::create_dir_all(&canon).unwrap();
    std::fs::write(canon.join("SKILL.md"), "g").unwrap();
    let mut reg = Registry::load(&paths).unwrap();
    reg.upsert(skillkit_core::registry::SkillMeta {
        id: "dc/g".into(), name: "gskill".into(), source: "dc".into(),
        scope: Scope::Global, version: None, commit_sha: Some("sha".into()),
        installed_at: "2026-07-29T00:00:00Z".into(),
        canonical_path: canon.to_string_lossy().into_owned(),
    });
    reg.save(&paths).unwrap();
    let mut proj = Project {
        name: "proj".into(), path: project_root.to_string_lossy().into_owned(),
        agents: vec!["claude-code".into()], applied_profiles: vec![],
        installed_skills: vec!["dc/g".into()], locked_shas: Default::default(),
    };
    run_apply(&paths, &mut proj, false).unwrap();
    // global 不在项目落地，但 Claude 全局 symlink 在位
    assert!(!project_root.join(".claude/skills/gskill").exists(), "global 不 per-project 落地");
    assert!(paths.claude_skills_dir().join("gskill").is_symlink(), "global 的 Claude symlink 在位");
}
```

- [ ] **Step 4: --json schema 锁定测试**

`m1_e2e.rs` 追加（防 schema 漂移）：
```rust
#[test]
fn json_schema_status_and_report_stable() {
    // ApplyReport 字段：created/removed/recopied/warnings（Vec<String>）
    let report = skillkit_core::apply::ApplyReport {
        created: vec!["a".into()], removed: vec![], recopied: vec![], warnings: vec!["w".into()],
    };
    let json = serde_json::to_value(&report).unwrap();
    let obj = json.as_object().unwrap();
    assert!(obj.contains_key("created") && obj.contains_key("removed")
        && obj.contains_key("recopied") && obj.contains_key("warnings"),
        "ApplyReport --json schema 必须稳定含这四个字段");
}
```

注：`ApplyReport` 需 `Serialize`（Task 7 已 derive）。StatusView 的 Serialize 已手写（Task 9）。

- [ ] **Step 5: 跑全量 + clippy**

Run: `make check`
Expected: core 单元 + m0_e2e + m1_e2e 全绿，clippy 零 warning。

- [ ] **Step 6: Commit**

```bash
git add crates/core/Cargo.toml crates/core/src/install.rs crates/cli/src/commands/install.rs crates/core/tests/m1_e2e.rs
git commit -m "test: M1 端到端 + --json schema 锁定；now_iso 接真实时间"
```

---

## M1 完成验收（spec §15.2）

跑完 10 个 task 后以下全部成立即 M1 达成：
- [ ] `skillkit profile create/add-skill/remove-skill/list` 可用。
- [ ] `skillkit project add`（生成随机 8 hex project-id）/ rebind（重绑定：id 不变、path/name 更新）/ scan / list 可用。
- [ ] `skillkit project apply-profile / add-skill / remove-skill` 增删 installed_skills。
- [ ] `skillkit project apply` 幂等落地（Claude symlink、Cursor copy），重复 apply 零变化。
- [ ] `skillkit project status [--json]` 输出 {expected, missing, extra, conflicts}。
- [ ] local skill 平铺在 `<project>/<agent>/skills/<skill>/`，`.git/info/exclude` 维护。
- [ ] global skill 不 per-project 落地，但 Claude 全局 symlink 在位。
- [ ] locked_shas 漂移检测，`--frozen` 报错。
- [ ] Cursor copy 副本过期（sha 漂移）自动重 copy。
- [ ] `cargo test` 全绿（M0 17 + M1 新增），clippy 零 warning。

不在 M1 范围（M2+）：`skillkit serve` + GUI、SSE、install upgrade/list、import-existing（M3）。

## Self-Review

**1. Spec 覆盖**（§8.4-§11、§15.2）：
- §8.4 profile → Task 1-2 ✓
- §8.5 project（installed_skills/locked_shas/applied_profiles）→ Task 3-5 ✓
- §9 profile/project 分工（apply-profile 批量、add-skill 精确）→ Task 5 ✓
- §10.1 三层语义（install/add-skill/apply）→ M0 install + Task 5 add-skill + Task 8 apply ✓
- §10.2 apply 流程（解析→diff→扫描→幂等→exclude→global 全局）→ Task 6-8 ✓
- §10.3 冲突检测（apply 时 frozen 报错、默认以 canonical 为准更新基线）→ Task 8 ✓
- §11 命令表面（profile/project 全命令 + --json + --frozen）→ Task 2/4/5/9 ✓
- §13 错误场景：shared 同名（land_one 警告）、Cursor copy 过期（Task 7 recopied）、未安装报错（Task 8）、dangling（land_one 读 symlink 失败走重建路径）→ 覆盖 ✓；local 已被 git 追踪（提示 git rm --cached）→ **部分覆盖**（Task 7 land_one 遇真实目录在 symlink 模式报错，但未显式提示 `git rm --cached`，留 M1 后期打磨或 M3）。
- §15.2 M1 交付 → 验收清单 ✓

**2. Placeholder 扫描**：无 TBD/TODO；每个 step 含完整代码或精确命令。（Task 9 status 已升级为 id 清单，满足 spec §11 agent 决策，非计数。）

**3. 类型一致**：`LocalTarget`（Task 6 定义）在 Task 7/8/9 复用一致；`ApplyDiff`/`ApplyReport`（Task 6/7）字段名跨 task 一致；`new_id`/`Project::{register,rebind,load,save}`/`Profile` 签名跨 task 一致；`Scope`/`SkillMeta` 复用 M0。已核。

**待主人确认的设计点**（计划内定档，可调）：
1. ~~`project_id` = path sanitize（可读）~~ → **已改（主人确认）：uuid v4 前 8 hex 短码，独立于 path；Project.id 注册时冻结，`project rebind <id> <new-path>` 更新 path/name、id 不变，支持项目移动/改名重绑定。**
2. Cursor copy 一致性用 `.skillkit-sha` 标记文件；若偏好别的（如 sidecar metadata），Task 7 改。
3. `install add` 保留（spec `install <id>` 的显式形式），不改命令。
4. status `--json` 给计数概览（extra 近似）；若要精确清单，Task 9 扩展。
