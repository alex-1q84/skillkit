# SkillKit M3 迁移打磨实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 skillkit 接住真实环境存量 skill（import-existing 扫描登记 + unmanaged 概念）、补齐升级闭环（upgrade <id> | --all）、并打包进 mac-config justfile。

**Architecture:** 三层共享 core。M3 新增 `crates/core/src/import.rs`（import_existing 主流程 + ImportReport）和 `crates/core/src/upgrade.rs`（upgrade_skill/upgrade_all + UpgradeReport），复用现有 `npx::update/read_computed_hash`、`Registry`、`Project.locked_shas`、`install`。unmanaged skill（存量目录无法溯源）以虚拟源 `unmanaged` 登记进 registry（`computed_hash=None` 不可升级、uninstall 不删目录）。CLI 加 `import-existing` / `upgrade` 顶层命令，GUI Skills 视图加 unmanaged 角标 + 升级按钮。

**Tech Stack:** Rust edition 2021 · clap(derive) · serde + serde_json · thiserror(core) + anyhow(cli) · chrono · tempfile(测试) · Askama(server)

## Global Constraints

（每个 task 的要求都隐含包含本节，来自 CLAUDE.md §5/§7/§8 + spec `2026-07-31-skillkit-m3-design.md`）

- Rust edition 2021，workspace resolver = "2"
- 路径绝不硬编码：用户目录一律 `Paths`（生产 `dirs::home_dir()`，测试 `Paths::new(tempdir)` 注入）
- `~/.agents/skills/` 只放全局公共 skill，绝不写元数据文件进去；元数据统一收 `~/.skillkit/`
- core 用 `thiserror` 定义错误类型，cli 顶层用 `anyhow` 聚合
- 错误信息遵循「反馈引导行动」，不静默跳过（skill 未安装就提示 `skillkit install <id>`）
- **core 公开类型一律在 `lib.rs` 完整 re-export**：新增 `ImportReport`/`UpgradeReport`/`import_existing`/`upgrade_skill`/`upgrade_all` 必须补进 lib.rs
- 状态文件原子写（写临时文件 + rename）；日志用 `tracing`，不裸 `println!`（CLI 用户可见输出可用 println）
- 注释中文；commit message 用 Conventional Commits 中文
- 测试验证业务结果（import 后 registry 有 unmanaged 记录、upgrade 后 hash 更新），不验证内部函数调用
- **unmanaged skill**：`source="unmanaged"`、`computed_hash=None`、`version=None`、`scope=global`；upgrade 跳过、uninstall 不删目录、profile 可引用
- **可溯源判定只认 `.git` + remote url 一种形态**；`~/.agents/skills/` 下不重装（重装自撞，见 spec §3）
- **upgrade 冲突判定**：受影响项目 = `locked_shas[id] == 当前 registry.computed_hash`（升级会让它们从同步变漂移）；`yes=false` 冲突返回 `SkillkitError::UpgradeBlocked`；core 不读 stdin，交互确认放 CLI 层
- 改完必跑 `make format && make lint`（`make check` 提交前一站式）；测试跑 `cargo test -p skillkit-core`

## File Structure

```
skillkit/
  crates/core/src/
    paths.rs                  # MODIFY: 加 codex_skills_dir()/cursor_skills_dir()
    error.rs                  # MODIFY: 加 SkillkitError::UpgradeBlocked
    install.rs                # MODIFY: uninstall 保护 unmanaged；now_iso 改 pub(crate)
    import.rs                 # CREATE: ImportReport + import_existing 主流程
    upgrade.rs                # CREATE: UpgradeReport + upgrade_skill/upgrade_all + 冲突检测
    lib.rs                    # MODIFY: re-export import/upgrade 模块 + 新类型
    tests/m3_e2e.rs           # CREATE: #[ignore] 端到端（import 真跑 + upgrade 真跑 npx）
  crates/cli/src/
    main.rs                   # MODIFY: Cmd 枚举加 ImportExisting/Upgrade
    commands/mod.rs           # MODIFY: 加 pub mod import; pub mod upgrade;
    commands/import.rs        # CREATE: ImportExistingCmd
    commands/upgrade.rs       # CREATE: UpgradeCmd
  crates/server/src/
    routes/mod.rs             # MODIFY: 加 /skills/{id}/upgrade 路由
    routes/skills.rs          # MODIFY: 加 upgrade handler
  crates/server/templates/
    fragments/skills_main.html # MODIFY: unmanaged 角标 + 升级按钮
  docs/
    2026-07-29-skillkit-design.md   # MODIFY: §11 命令表加 import-existing/upgrade
    sessions/2026-07-29-skillkit-design.md  # MODIFY: §1.1 命令表面
/Users/mywo/lab/mac-config/
  justfile                   # MODIFY: 加 install_skillkit recipe
```

## 模块接口速查（后任务引用）

```rust
// import.rs
pub struct ImportReport {
    pub imported: Vec<String>,
    pub unmanaged: Vec<String>,
    pub reinstalled: Vec<String>,
    pub skipped: Vec<String>,
}
pub fn import_existing(paths: &Paths, dry_run: bool) -> Result<ImportReport>;

// upgrade.rs
pub struct UpgradeReport {
    pub id: String,
    pub old_hash: String,
    pub new_hash: String,
    pub affected_projects: Vec<String>,
}
pub fn upgrade_skill(paths: &Paths, id: &str, yes: bool) -> Result<UpgradeReport>;
pub fn upgrade_all(paths: &Paths, yes: bool) -> Result<Vec<UpgradeReport>>;

// error.rs 新增变体
SkillkitError::UpgradeBlocked { id: String, affected: Vec<String> }

// install.rs
pub(crate) fn now_iso() -> String;   // 原私有 fn 改 pub(crate)，import/upgrade 复用
```

---

### Task 1: core Paths 加 codex/cursor 历史私有目录

**Files:**
- Modify: `crates/core/src/paths.rs`
- Test: `crates/core/src/paths.rs`（模块内 tests 补两个断言）

**Interfaces:**
- Produces: `Paths::codex_skills_dir() -> PathBuf`（`~/.codex/skills`）、`Paths::cursor_skills_dir() -> PathBuf`（`~/.cursor/skills`）

- [ ] **Step 1: 写失败测试**

在 `paths.rs` 的 `#[cfg(test)] mod tests` 的 `production_and_injected_share_layout` 函数末尾追加：

```rust
        assert_eq!(
            p.codex_skills_dir(),
            PathBuf::from("/tmp/fakehome/.codex/skills")
        );
        assert_eq!(
            p.cursor_skills_dir(),
            PathBuf::from("/tmp/fakehome/.cursor/skills")
        );
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-core paths::tests`
Expected: 编译错误 `no method named codex_skills_dir` / `cursor_skills_dir`

- [ ] **Step 3: 写最小实现**

在 `paths.rs` 的 `impl Paths` 里、`claude_skills_dir()` 之后加：

```rust
    /// Codex 历史私有目录（import-existing 扫描用；新设计下 agent 直读 ~/.agents/skills/）。
    pub fn codex_skills_dir(&self) -> PathBuf {
        self.home.join(".codex").join("skills")
    }

    /// Cursor 历史私有目录。
    pub fn cursor_skills_dir(&self) -> PathBuf {
        self.home.join(".cursor").join("skills")
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p skillkit-core paths::tests`
Expected: 全 PASS（2 个新断言 + 原有断言）

- [ ] **Step 5: 跑 lint 确认干净**

Run: `cargo clippy -p skillkit-core --all-targets -- -D warnings`
Expected: 零 warning

- [ ] **Step 6: Commit**

```bash
git add crates/core/src/paths.rs
git commit -m "feat(core): Paths 加 codex/cursor 历史私有目录（import-existing 扫描用）"
```

---

### Task 2: error 加 UpgradeBlocked + uninstall 保护 unmanaged

**Files:**
- Modify: `crates/core/src/error.rs`
- Modify: `crates/core/src/install.rs`
- Test: `crates/core/src/install.rs`（模块内无 tests，改用 `crates/core/tests/` 的临时集成测试——本任务在 `install.rs` 内加 `#[cfg(test)]` 模块）

**Interfaces:**
- Produces: `SkillkitError::UpgradeBlocked { id: String, affected: Vec<String> }`；`install.rs::uninstall` 对 `computed_hash=None` 的 skill 不删 canonical 目录、不调 `npx::remove`；`install::now_iso` 变 `pub(crate)`

- [ ] **Step 1: 写失败测试（uninstall 不删 unmanaged 目录）**

在 `crates/core/src/install.rs` 文件末尾追加 `#[cfg(test)] mod tests`：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::paths::Paths;
    use crate::registry::{Registry, Scope, SkillMeta};
    use tempfile::tempdir;

    /// unmanaged skill（computed_hash=None）uninstall 时：只摘 registry 记录，不删 canonical 目录。
    #[test]
    fn uninstall_unmanaged_keeps_directory() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());

        // 存量真实目录（模拟 ~/.agents/skills/foo，用户手工放置）
        let canon = tmp.path().join(".agents/skills/foo");
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();

        let mut reg = Registry::load(&paths).unwrap();
        reg.upsert(SkillMeta {
            id: "unmanaged/foo".into(),
            name: "foo".into(),
            source: "unmanaged".into(),
            scope: Scope::Global,
            version: None,
            computed_hash: None,
            installed_at: "2026-07-31T00:00:00Z".into(),
            canonical_path: canon.to_string_lossy().into_owned(),
        });
        reg.save(&paths).unwrap();

        uninstall(&paths, "unmanaged/foo").unwrap();

        assert!(canon.exists(), "unmanaged 的目录不能被删");
        assert!(Registry::load(&paths).unwrap().get("unmanaged/foo").is_err());
    }

    /// managed skill（computed_hash=Some）uninstall 仍删 canonical 目录（行为不变）。
    #[test]
    fn uninstall_managed_still_removes_directory() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        let canon = paths.skillkit_skills_dir().join("foo");
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();

        let mut reg = Registry::load(&paths).unwrap();
        reg.upsert(SkillMeta {
            id: "skills.sh/foo".into(),
            name: "foo".into(),
            source: "skills.sh".into(),
            scope: Scope::Global,
            version: None,
            computed_hash: Some("abc123".into()),
            installed_at: "2026-07-31T00:00:00Z".into(),
            canonical_path: canon.to_string_lossy().into_owned(),
        });
        reg.save(&paths).unwrap();

        uninstall(&paths, "skills.sh/foo").unwrap();
        assert!(!canon.exists(), "managed 的 canonical 目录应被删");
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-core uninstall_unmanaged_keeps_directory`
Expected: FAIL（当前 uninstall 会删目录，`canon.exists()` 为 false）

- [ ] **Step 3: error.rs 加 UpgradeBlocked 变体**

在 `crates/core/src/error.rs` 的 `SkillkitError` 枚举末尾（`LockTimeout` 之后）加：

```rust
    #[error("升级 {id} 将影响 {n} 个项目的版本基线（{affected}），需确认或 --yes")]
    UpgradeBlocked {
        id: String,
        affected: Vec<String>,
    },
```

- [ ] **Step 4: install.rs 改 uninstall 保护 unmanaged + now_iso 改 pub(crate)**

`crates/core/src/install.rs` 的 `uninstall` 函数改为：

```rust
/// 卸载：managed 删 canonical 池子 + 同步 npx skills lock；unmanaged（computed_hash=None）
/// 只摘 registry 记录，不删目录（不是 skillkit 装的，避免误删用户手工放置的 skill）。
pub fn uninstall(paths: &Paths, id: &str) -> Result<()> {
    let mut reg = Registry::load(paths)?;
    let meta = reg.get(id)?.clone();
    if meta.computed_hash.is_some() {
        let target = PathBuf::from(&meta.canonical_path);
        if target.exists() {
            std::fs::remove_dir_all(&target)
                .map_err(|_| SkillkitError::CanonicalCreate(target.clone()))?;
        }
        let _ = npx::remove(paths, &meta.name); // 同步 lock，失败不阻塞（registry 是事实源）
    }
    reg.remove(id)?.save(paths)?;
    Ok(())
}
```

`now_iso` 的 `fn` 改 `pub(crate) fn`：

```rust
/// 当前时间 ISO 字符串（UTC RFC3339）。
pub(crate) fn now_iso() -> String {
    chrono::Utc::now().to_rfc3339()
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p skillkit-core uninstall`
Expected: 两个新测试 PASS，原有测试（含 `install.rs` 依赖）不受影响

- [ ] **Step 6: 跑 lint 确认干净**

Run: `cargo clippy -p skillkit-core --all-targets -- -D warnings`
Expected: 零 warning（注意 `n` 未用时用 `{n}` 在 Display 里会自动算 `affected.len()`，thiserror 字段需在格式化串里出现才会视为用；若 clippy 报 `n` 未用，改 Display 为 `{affected}` 不用 `{n}`）

- [ ] **Step 7: Commit**

```bash
git add crates/core/src/error.rs crates/core/src/install.rs
git commit -m "fix(core): uninstall 保护 unmanaged skill（不删存量目录）+ UpgradeBlocked 错误"
```

---

### Task 3: core import.rs —— import_existing 主流程

**Files:**
- Create: `crates/core/src/import.rs`
- Modify: `crates/core/src/lib.rs`（re-export）
- Test: `crates/core/src/import.rs`（模块内 `#[cfg(test)]` 集成测试，tempdir）

**Interfaces:**
- Consumes: `Paths::codex_skills_dir()` / `cursor_skills_dir()`（Task 1）、`install::now_iso()`（Task 2 pub(crate)）、`Registry::skill_id` / `upsert` / `save`、`SourcesStore::get/add/save`、`derive_source_name`
- Produces: `ImportReport { imported, unmanaged, reinstalled, skipped }`、`import_existing(paths, dry_run) -> Result<ImportReport>`

- [ ] **Step 1: 写失败测试（unmanaged 登记 + symlink/无效跳过 + 幂等 + dry-run）**

创建 `crates/core/src/import.rs`，先写模块骨架 + 测试：

```rust
//! import-existing：扫描存量 skill 目录（~/.agents/skills、~/.claude/skills、
//! ~/.codex/skills、~/.cursor/skills），识别 + 登记进 registry。
//! 无源 → unmanaged（虚拟源，computed_hash=None，不可升级）；有 .git 可溯源 → 重装入池。
use crate::error::{Result, SkillkitError};
use crate::paths::Paths;
use crate::registry::{Registry, Scope, SkillMeta};
use crate::source::{derive_source_name, Source, SourcesStore};
use std::path::Path;

/// import 结果汇总。
#[derive(Debug, Clone, Default, serde::Serialize)]
pub struct ImportReport {
    /// 成功登记（含 unmanaged）的 skill 名。
    pub imported: Vec<String>,
    /// 以 unmanaged 登记的数量。
    pub unmanaged: Vec<String>,
    /// 可溯源并重装入池的数量。
    pub reinstalled: Vec<String>,
    /// 跳过的（重复 / 无效 / symlink / 无 SKILL.md / 重装撞占位）。
    pub skipped: Vec<String>,
}

pub fn import_existing(paths: &Paths, dry_run: bool) -> Result<ImportReport> {
    todo!()
}

/// 扫描一个目录：收集 (name, canonical_path, package?)。SKILL.md 存在才算 skill。
fn scan_dir(
    dir: Path,
    skip_symlink: bool,
    allow_reinstall: bool,
    plan: &mut Vec<(String, String, Option<String>)>,
    skipped: &mut Vec<String>,
) {
    todo!()
}

/// 读 git remote url（溯源 package）。失败返回 None。
fn read_git_remote(dir: &Path) -> Option<String> {
    todo!()
}

/// 重装入池：derive source name → 注册 source → install Global。撞占位/下载失败 → Err。
fn try_reinstall(paths: &Paths, name: &str, package: &str) -> Result<()> {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::tempdir;

    fn make_skill(dir: &Path, name: &str) {
        let d = dir.join(name);
        std::fs::create_dir_all(&d).unwrap();
        std::fs::write(d.join("SKILL.md"), format!("---\nname: {name}\n---\n# {name}\n")).unwrap();
    }

    #[test]
    fn import_registers_unmanaged_and_skips_invalid() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());

        // 有效的：agents/foo、codex/bar、claude/baz（真实目录）
        make_skill(&paths.agents_skills_dir(), "foo");
        make_skill(&paths.codex_skills_dir(), "bar");
        make_skill(&paths.claude_skills_dir(), "baz");

        // 无效的：claude 下的 symlink（指向 agents/foo）、无 SKILL.md 目录、空目录
        std::fs::create_dir_all(&paths.claude_skills_dir()).unwrap();
        std::os::unix::fs::symlink(
            paths.agents_skills_dir().join("foo"),
            paths.claude_skills_dir().join("foo-link"),
        )
        .unwrap();
        let no_md = paths.codex_skills_dir().join("no-md");
        std::fs::create_dir_all(&no_md).unwrap();
        let empty = paths.cursor_skills_dir().join("empty");
        std::fs::create_dir_all(&empty).unwrap();

        let report = import_existing(&paths, false).unwrap();

        // foo/bar/baz 登记为 unmanaged
        assert!(report.imported.contains(&"foo".to_string()));
        assert!(report.imported.contains(&"bar".to_string()));
        assert!(report.imported.contains(&"baz".to_string()));
        assert_eq!(report.unmanaged.len(), 3);

        let reg = Registry::load(&paths).unwrap();
        let foo = reg.get("unmanaged/foo").unwrap();
        assert_eq!(foo.source, "unmanaged");
        assert!(foo.computed_hash.is_none());
        assert_eq!(foo.scope, Scope::Global);
        let baz = reg.get("unmanaged/baz").unwrap();
        assert_eq!(baz.canonical_path, paths.claude_skills_dir().join("baz").to_string_lossy());

        // symlink 跳过（claude 里只有 baz 登记，foo-link 没有）
        assert!(reg.get("unmanaged/foo-link").is_err());
        // 无 SKILL.md / 空目录跳过
        assert!(reg.get("unmanaged/no-md").is_err());
        assert!(reg.get("unmanaged/empty").is_err());
    }

    #[test]
    fn import_dry_run_writes_nothing() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        make_skill(&paths.agents_skills_dir(), "foo");

        let report = import_existing(&paths, true).unwrap();
        assert!(report.unmanaged.contains(&"foo".to_string()));
        assert!(Registry::load(&paths).unwrap().skills.is_empty(), "dry-run 不写 registry");
    }

    #[test]
    fn import_is_idempotent() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        make_skill(&paths.agents_skills_dir(), "foo");

        let r1 = import_existing(&paths, false).unwrap();
        assert_eq!(r1.imported.len(), 1);

        let r2 = import_existing(&paths, false).unwrap();
        assert!(r2.imported.is_empty(), "重复跑不重复登记");
        assert!(r2.skipped.contains(&"foo".to_string()));
        assert_eq!(Registry::load(&paths).unwrap().skills.len(), 1);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-core import::tests`
Expected: 编译过但 `todo!()` panic / 断言失败

- [ ] **Step 3: 写完整实现**

用以下完整实现替换 `todo!()`（保留 Step 1 的测试部分不变）：

```rust
pub fn import_existing(paths: &Paths, dry_run: bool) -> Result<ImportReport> {
    let mut report = ImportReport::default();
    let reg = Registry::load(paths)?;
    let mut registered: std::collections::HashSet<String> =
        reg.skills.values().map(|m| m.name.clone()).collect();

    let mut plan: Vec<(String, String, Option<String>)> = Vec::new(); // (name, canonical, package?)

    // 1. ~/.agents/skills：全局落地点本体，一律 unmanaged（重装会自撞自身占位，无意义）
    scan_dir(paths.agents_skills_dir(), false, false, &mut plan, &mut report.skipped);
    // 2. ~/.claude/skills：跳过 symlink，真实目录 unmanaged
    scan_dir(paths.claude_skills_dir(), true, false, &mut plan, &mut report.skipped);
    // 3/4. codex/cursor：有 .git 尝试重装，否则 unmanaged
    scan_dir(paths.codex_skills_dir(), true, true, &mut plan, &mut report.skipped);
    scan_dir(paths.cursor_skills_dir(), true, true, &mut plan, &mut report.skipped);

    for (name, canonical, package) in plan {
        if registered.contains(&name) {
            report.skipped.push(name);
            continue;
        }
        if let Some(pkg) = package {
            if dry_run {
                report.reinstalled.push(name.clone());
                report.imported.push(name);
                continue;
            }
            match try_reinstall(paths, &name, &pkg) {
                Ok(()) => {
                    report.reinstalled.push(name.clone());
                    registered.insert(name.clone());
                }
                Err(e) => {
                    tracing::warn!(error = ?e, "重装 {name} 失败，保留原状");
                    report.skipped.push(name.clone());
                    continue;
                }
            }
        } else if !dry_run {
            let mut reg = Registry::load(paths)?;
            reg.upsert(SkillMeta {
                id: Registry::skill_id("unmanaged", &name),
                name: name.clone(),
                source: "unmanaged".into(),
                scope: Scope::Global,
                version: None,
                computed_hash: None,
                installed_at: crate::install::now_iso(),
                canonical_path: canonical.clone(),
            });
            reg.save(paths)?;
            registered.insert(name.clone());
            report.unmanaged.push(name.clone());
        } else {
            report.unmanaged.push(name.clone());
        }
        report.imported.push(name);
    }
    Ok(report)
}

fn scan_dir(
    dir: Path,
    skip_symlink: bool,
    allow_reinstall: bool,
    plan: &mut Vec<(String, String, Option<String>)>,
    skipped: &mut Vec<String>,
) {
    let Ok(entries) = std::fs::read_dir(&dir) else {
        return; // 目录不存在 / 无权限：不是错误，静默跳过
    };
    for entry in entries.flatten() {
        let p = entry.path();
        if !p.is_dir() {
            continue;
        }
        if skip_symlink && p.is_symlink() {
            continue;
        }
        let name = entry.file_name().to_string_lossy().into_owned();
        if !p.join("SKILL.md").exists() {
            skipped.push(format!("{name}（无 SKILL.md）"));
            continue;
        }
        let package = if allow_reinstall && p.join(".git").exists() {
            read_git_remote(&p)
        } else {
            None
        };
        plan.push((name, p.to_string_lossy().into_owned(), package));
    }
}

fn read_git_remote(dir: &Path) -> Option<String> {
    let out = std::process::Command::new("git")
        .args(["remote", "get-url", "origin"])
        .current_dir(dir)
        .output()
        .ok()?;
    if !out.status.success() {
        return None;
    }
    let url = String::from_utf8_lossy(&out.stdout).trim().to_string();
    if url.is_empty() {
        None
    } else {
        Some(url)
    }
}

fn try_reinstall(paths: &Paths, name: &str, package: &str) -> Result<()> {
    let source_name = derive_source_name(package)
        .ok_or_else(|| SkillkitError::Tool { message: format!("无法从 {package} 推导源名") })?;
    let mut store = SourcesStore::load(paths)?;
    if store.get(&source_name).is_err() {
        store.add(Source {
            name: source_name.clone(),
            package: Some(package.into()),
        })?;
        store.save(paths)?;
    }
    crate::install::install(paths, &source_name, name, package, Scope::Global)?;
    Ok(())
}
```

- [ ] **Step 4: lib.rs re-export（只加 import 模块）**

在 `crates/core/src/lib.rs` 的 `pub mod` 列表加 `pub mod import;`（**注意：upgrade 模块是 Task 4 才建，本步先不加，避免编译断**），并在 `pub use` 追加：

```rust
pub use import::{import_existing, ImportReport};
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p skillkit-core import::tests`
Expected: 3 个测试全 PASS

- [ ] **Step 6: 跑 lint 确认干净**

Run: `cargo clippy -p skillkit-core --all-targets -- -D warnings`
Expected: 零 warning（注意 `report.unmanaged.push` 在两个分支各出现一次，dry_run 分支逻辑正确）

- [ ] **Step 7: Commit**

```bash
git add crates/core/src/import.rs crates/core/src/lib.rs
git commit -m "feat(core): import_existing 扫描存量 skill 目录登记 unmanaged（dry-run/幂等）"
```

---

### Task 4: core upgrade.rs —— upgrade_skill / upgrade_all

**Files:**
- Create: `crates/core/src/upgrade.rs`
- Modify: `crates/core/src/lib.rs`（re-export upgrade 模块）
- Test: `crates/core/src/upgrade.rs`（模块内 `#[cfg(test)]`）+ `crates/core/tests/m3_e2e.rs`（`#[ignore]` 端到端）

**Interfaces:**
- Consumes: `SkillkitError::UpgradeBlocked`（Task 2）、`npx::update` / `npx::read_computed_hash`、`Registry`、`project::list_ids` / `project::Project::load`、`install::now_iso`
- Produces: `UpgradeReport { id, old_hash, new_hash, affected_projects }`、`upgrade_skill(paths, id, yes)`、`upgrade_all(paths, yes)`

- [ ] **Step 1: 写失败测试（错误路径 + 冲突检测）**

创建 `crates/core/src/upgrade.rs`，先写骨架 + 测试：

```rust
//! upgrade：升级已安装 skill（npx skills update），更新 registry.computed_hash。
//! 冲突检测：升级会让「锁了当前 hash」的项目从同步变漂移，yes=false 时返回 UpgradeBlocked。
use crate::error::{Result, SkillkitError};
use crate::paths::Paths;
use crate::registry::Registry;

/// 单次升级结果。
#[derive(Debug, Clone, serde::Serialize)]
pub struct UpgradeReport {
    pub id: String,
    /// 升级前 computed_hash。
    pub old_hash: String,
    /// 升级后 computed_hash。
    pub new_hash: String,
    /// 升级后从「同步」变「漂移」的项目 id（需重新 project apply）。
    pub affected_projects: Vec<String>,
}

pub fn upgrade_skill(paths: &Paths, id: &str, yes: bool) -> Result<UpgradeReport> {
    todo!()
}

/// 升级全部 registry skill。unmanaged / 未安装 / 冲突 / 下载失败的跳过并 warn，不中断。
pub fn upgrade_all(paths: &Paths, yes: bool) -> Result<Vec<UpgradeReport>> {
    todo!()
}

/// 找「升级后受影响」的项目：locked_shas[id] == old_hash（当前同步，升级后会漂移）。
fn find_affected_projects(paths: &Paths, id: &str, old_hash: &str) -> Result<Vec<String>> {
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::project::Project;
    use crate::registry::{Scope, SkillMeta};
    use tempfile::tempdir;
    use std::collections::BTreeMap;

    fn install_managed(paths: &Paths, id: &str, hash: &str) {
        let skill = id.split('/').next_back().unwrap_or(id);
        let canon = paths.skillkit_skills_dir().join(skill);
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();
        let mut reg = Registry::load(paths).unwrap();
        reg.upsert(SkillMeta {
            id: id.into(),
            name: skill.into(),
            source: id.split('/').next().unwrap_or("").into(),
            scope: Scope::Local,
            version: None,
            computed_hash: Some(hash.into()),
            installed_at: "2026-07-31T00:00:00Z".into(),
            canonical_path: canon.to_string_lossy().into_owned(),
        });
        reg.save(paths).unwrap();
    }

    fn install_unmanaged(paths: &Paths, name: &str) {
        let canon = paths.agents_skills_dir().join(name);
        std::fs::create_dir_all(&canon).unwrap();
        std::fs::write(canon.join("SKILL.md"), "x").unwrap();
        let mut reg = Registry::load(paths).unwrap();
        reg.upsert(SkillMeta {
            id: format!("unmanaged/{name}"),
            name: name.into(),
            source: "unmanaged".into(),
            scope: Scope::Global,
            version: None,
            computed_hash: None,
            installed_at: "2026-07-31T00:00:00Z".into(),
            canonical_path: canon.to_string_lossy().into_owned(),
        });
        reg.save(paths).unwrap();
    }

    fn save_project(paths: &Paths, id: &str, locked: &[(&str, &str)]) {
        let mut proj = Project {
            id: id.into(),
            name: id.into(),
            path: "/tmp/p".into(),
            agents: vec![],
            applied_profiles: vec![],
            installed_skills: vec![],
            locked_shas: locked.iter().map(|(k, v)| ((*k).to_owned(), (*v).to_owned())).collect(),
        };
        proj.save(paths).unwrap();
    }

    #[test]
    fn upgrade_unknown_skill_errors() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        let err = upgrade_skill(&paths, "nope/x", true).unwrap_err();
        assert!(matches!(err, SkillkitError::SkillNotInstalled { .. }));
    }

    #[test]
    fn upgrade_unmanaged_skips() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        install_unmanaged(&paths, "foo");
        let err = upgrade_skill(&paths, "unmanaged/foo", true).unwrap_err();
        assert!(matches!(err, SkillkitError::Tool { .. }));
    }

    #[test]
    fn upgrade_blocked_when_project_locked_and_no_yes() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        install_managed(&paths, "dc/foo", "oldhash");
        save_project(&paths, "P1", &[("dc/foo", "oldhash")]); // 锁当前 hash → 升级会漂移
        save_project(&paths, "P2", &[("dc/foo", "other")]); // 锁别的 → 不受影响

        let err = upgrade_skill(&paths, "dc/foo", false).unwrap_err();
        match err {
            SkillkitError::UpgradeBlocked { id, affected } => {
                assert_eq!(id, "dc/foo");
                assert_eq!(affected, vec!["P1".to_string()]);
            }
            other => panic!("expected UpgradeBlocked, got {other:?}"),
        }
    }

    #[test]
    fn find_affected_matches_only_locked_current_hash() {
        let tmp = tempdir().unwrap();
        let paths = Paths::new(tmp.path().to_path_buf());
        save_project(&paths, "P1", &[("dc/foo", "oldhash")]);
        save_project(&paths, "P2", &[("dc/bar", "other")]);
        save_project(&paths, "P3", &[("dc/foo", "other")]);
        let affected = find_affected_projects(&paths, "dc/foo", "oldhash").unwrap();
        assert_eq!(affected, vec!["P1".to_string()]);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-core upgrade::tests`
Expected: `todo!()` panic（测试逻辑能编译但运行失败）

- [ ] **Step 3: 写完整实现**

替换 `todo!()`：

```rust
pub fn upgrade_skill(paths: &Paths, id: &str, yes: bool) -> Result<UpgradeReport> {
    let mut reg = Registry::load(paths)?;
    let mut meta = reg.get(id)?.clone();
    let old_hash = meta.computed_hash.clone().ok_or_else(|| SkillkitError::Tool {
        message: format!("{id} 是 unmanaged skill，无版本锁，无法升级"),
    })?;
    let affected = find_affected_projects(paths, id, &old_hash)?;
    if !affected.is_empty() && !yes {
        return Err(SkillkitError::UpgradeBlocked {
            id: id.to_string(),
            affected,
        });
    }
    crate::npx::update(paths, &meta.name)?;
    let new_hash = crate::npx::read_computed_hash(paths, &meta.name)?;
    meta.computed_hash = Some(new_hash.clone());
    meta.installed_at = crate::install::now_iso();
    reg.upsert(meta);
    reg.save(paths)?;
    Ok(UpgradeReport {
        id: id.to_string(),
        old_hash,
        new_hash,
        affected_projects: affected,
    })
}

pub fn upgrade_all(paths: &Paths, yes: bool) -> Result<Vec<UpgradeReport>> {
    let reg = Registry::load(paths)?;
    let ids: Vec<String> = reg.skills.keys().cloned().collect();
    let mut reports = Vec::new();
    for id in ids {
        match upgrade_skill(paths, &id, yes) {
            Ok(r) => reports.push(r),
            Err(e) => tracing::warn!(error = ?e, "upgrade 跳过 {id}"),
        }
    }
    Ok(reports)
}

fn find_affected_projects(paths: &Paths, id: &str, old_hash: &str) -> Result<Vec<String>> {
    let mut affected = Vec::new();
    for pid in crate::project::list_ids(paths)? {
        let p = crate::project::Project::load(paths, &pid)?;
        if p.locked_shas.get(id).map(String::as_str) == Some(old_hash) {
            affected.push(pid);
        }
    }
    affected.sort();
    Ok(affected)
}
```

- [ ] **Step 4: lib.rs re-export + upgrade 模块声明**

`crates/core/src/lib.rs` 的 `pub mod` 列表加 `pub mod upgrade;`，`pub use` 追加：

```rust
pub use upgrade::{upgrade_all, upgrade_skill, UpgradeReport};
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p skillkit-core upgrade::tests`
Expected: 4 个测试全 PASS

- [ ] **Step 6: 写 `#[ignore]` 端到端（install → upgrade 真跑 npx）**

创建 `crates/core/tests/m3_e2e.rs`：

```rust
//! M3 端到端（#[ignore] 真跑 npx skills）：
//! import 溯源重装（local fixture 带 .git）+ upgrade 走 npx update 更新 hash。
use skillkit_core::{
    install, upgrade_skill,
    paths::Paths,
    registry::Registry,
    source::{Source, SourcesStore},
    Scope,
};
use tempfile::tempdir;
use std::process::Command;

/// 建带 .git 的本地 fixture 仓库（含 <skill>/SKILL.md），返回仓库根。
/// git 操作本地 bare repo 真跑（CLAUDE.md §8：不 mock）。
fn git_fixture(dir: &std::path::Path, skill: &str) -> std::path::PathBuf {
    let repo = dir.join("skill-repo");
    let skill_dir = repo.join(skill);
    std::fs::create_dir_all(&skill_dir).unwrap();
    std::fs::write(
        skill_dir.join("SKILL.md"),
        format!("---\nname: {skill}\ndescription: m3 fixture\n---\n# {skill}\n"),
    )
    .unwrap();
    Command::new("git")
        .args(["init", "-q"])
        .current_dir(&repo)
        .status()
        .unwrap();
    Command::new("git")
        .args(["config", "user.email", "m3@test"])
        .current_dir(&repo)
        .status()
        .unwrap();
    Command::new("git")
        .args(["config", "user.name", "m3"])
        .current_dir(&repo)
        .status()
        .unwrap();
    Command::new("git")
        .args(["add", "."])
        .current_dir(&repo)
        .status()
        .unwrap();
    Command::new("git")
        .args(["commit", "-q", "-m", "init"])
        .current_dir(&repo)
        .status()
        .unwrap();
    repo
}

#[test]
#[ignore = "install/upgrade 委托 npx skills，需本地 Node；cargo test -- --ignored 手动跑"]
fn upgrade_updates_registry_hash() {
    let tmp = tempdir().unwrap();
    let paths = Paths::new(tmp.path().to_path_buf());
    let repo = git_fixture(tmp.path(), "m3-demo");
    let pkg = repo.to_string_lossy().into_owned();

    let mut store = SourcesStore::default();
    store
        .add(Source {
            name: "m3-src".into(),
            package: Some(pkg.clone()),
        })
        .unwrap();
    store.save(&paths).unwrap();

    let meta = install(&paths, "m3-src", "m3-demo", &pkg, Scope::Local).unwrap();
    let old = meta.computed_hash.unwrap();

    let report = upgrade_skill(&paths, "m3-src/m3-demo", true).unwrap();
    assert_eq!(report.old_hash, old);
    assert!(!report.new_hash.is_empty());
    // registry 已更新为新 hash
    let reg = Registry::load(&paths).unwrap();
    assert_eq!(reg.get("m3-src/m3-demo").unwrap().computed_hash.as_deref(), Some(report.new_hash.as_str()));
}
```

- [ ] **Step 7: 跑端到端确认通过**

Run: `cargo test -p skillkit-core --test m3_e2e -- --ignored --nocapture`
Expected: `upgrade_updates_registry_hash` PASS（若 npx skills update 对 local source 行为异常导致失败，记录并调整断言，不删测试）

- [ ] **Step 8: 跑全量 + lint**

Run: `make format && cargo test -p skillkit-core && cargo clippy -p skillkit-core --all-targets -- -D warnings`
Expected: 全 PASS + 零 warning

- [ ] **Step 9: Commit**

```bash
git add crates/core/src/upgrade.rs crates/core/src/lib.rs crates/core/tests/m3_e2e.rs
git commit -m "feat(core): upgrade_skill/upgrade_all + locked_shas 冲突检测（UpgradeBlocked）"
```

---

### Task 5: CLI import-existing 命令

**Files:**
- Create: `crates/cli/src/commands/import.rs`
- Modify: `crates/cli/src/commands/mod.rs`、`crates/cli/src/main.rs`
- Test: `crates/cli/src/commands/import.rs`（clap 解析测试）

**Interfaces:**
- Consumes: `import_existing(paths, dry_run)` + `ImportReport`（Task 3）
- Produces: `ImportExistingCmd`（clap Args：`--dry-run` / `--json`）、main.rs `Cmd::ImportExisting`

- [ ] **Step 1: 写失败测试**

创建 `crates/cli/src/commands/import.rs`：

```rust
//! import-existing 子命令：扫存量 skill 目录登记进 registry（dry-run 只输出不写）。
use clap::Args;
use skillkit_core::{import_existing, paths::Paths};

#[derive(Args)]
pub struct ImportExistingCmd {
    /// 只输出不写 registry
    #[arg(long)]
    dry_run: bool,
    /// JSON 输出 ImportReport
    #[arg(long)]
    json: bool,
}

pub fn run(cmd: ImportExistingCmd) -> anyhow::Result<()> {
    let paths = Paths::production();
    let report = import_existing(&paths, cmd.dry_run)?;
    if cmd.json {
        println!("{}", serde_json::to_string_pretty(&report)?);
    } else {
        println!(
            "imported {}，unmanaged {}，reinstalled {}，skipped {}",
            report.imported.len(),
            report.unmanaged.len(),
            report.reinstalled.len(),
            report.skipped.len()
        );
        for s in &report.skipped {
            println!("  - {s}");
        }
        if cmd.dry_run {
            println!("（dry-run，未写入 registry）");
        }
    }
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;
    use clap::Parser;

    #[derive(Parser)]
    struct TestCli {
        #[command(subcommand)]
        cmd: ImportExistingCmd,
    }

    #[test]
    fn import_existing_parses_dry_run_and_json() {
        let TestCli { cmd } = TestCli::parse_from(["skillkit", "import-existing", "--dry-run", "--json"]);
        assert!(cmd.dry_run);
        assert!(cmd.json);
    }

    #[test]
    fn import_existing_defaults_false() {
        let TestCli { cmd } = TestCli::parse_from(["skillkit", "import-existing"]);
        assert!(!cmd.dry_run);
        assert!(!cmd.json);
    }
}
```

注意：TestCli 的 subcommand 包一层 `ImportExistingCmd`——但 main.rs 里 `Cmd::ImportExisting(ImportExistingCmd)` 是直接 Args 变体（不是再包 Subcommand）。TestCli 用 `#[command(subcommand)] cmd: ImportExistingCmd` 无法解析——因为 ImportExistingCmd 是 Args 不是 Subcommand。参照 cli/src/commands/install.rs 的测试模式：那里 `InstallCmd` 内含 `#[command(subcommand)] cmd: InstallSub`，TestCli 直接解析 `InstallSub`。

对 ImportExistingCmd（无内层 subcommand，纯 Args），测试应直接 `ImportExistingCmd::parse_from(...)`：

```rust
    #[test]
    fn import_existing_parses_dry_run_and_json() {
        let cmd = ImportExistingCmd::parse_from(["skillkit", "import-existing", "--dry-run", "--json"]);
        assert!(cmd.dry_run);
        assert!(cmd.json);
    }

    #[test]
    fn import_existing_defaults_false() {
        let cmd = ImportExistingCmd::parse_from(["skillkit", "import-existing"]);
        assert!(!cmd.dry_run);
        assert!(!cmd.json);
    }
```

（测试部分用这个版本，去掉 TestCli 包装。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-cli import_existing`
Expected: 编译失败（`ImportExistingCmd` 未定义）

- [ ] **Step 3: mod.rs + main.rs 接线（只加 import-existing，upgrade 留 Task 6）**

`crates/cli/src/commands/mod.rs` 加：

```rust
pub mod import;
```

（`pub mod upgrade;` 在 Task 6 加，避免本 task 引用未建模块编译断。）

`crates/cli/src/main.rs` 改：

```rust
use commands::import::{run as run_import, ImportExistingCmd};

#[derive(Subcommand)]
enum Cmd {
    // ... 现有变体 ...
    /// 扫描导入现有 skill（存量目录登记进 registry）
    ImportExisting(ImportExistingCmd),
}

match cli.cmd {
    // ... 现有 match 分支 ...
    Cmd::ImportExisting(cmd) => run_import(cmd)?,
}
```

（upgrade 的 `use` / `Cmd::Upgrade` 变体 / match 分支留 Task 6 一并加。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p skillkit-cli`
Expected: 2 个解析测试 PASS

- [ ] **Step 5: 手工验证 dry-run 输出（真实环境只读）**

Run: `cargo run -p skillkit-cli -- import-existing --dry-run`
Expected: 打印汇总（不写 registry），能看到 `~/.agents/skills/` 的 63 个存量被列为 unmanaged

- [ ] **Step 6: 跑 lint + 全量**

Run: `make format && cargo clippy -p skillkit-cli --all-targets -- -D warnings && cargo test -p skillkit-cli`
Expected: 零 warning + 全 PASS

- [ ] **Step 7: Commit**

```bash
git add crates/cli/src/commands/import.rs crates/cli/src/commands/mod.rs crates/cli/src/main.rs
git commit -m "feat(cli): import-existing 命令（--dry-run/--json，调 core import_existing）"
```

---

### Task 6: CLI upgrade 命令

**Files:**
- Create: `crates/cli/src/commands/upgrade.rs`
- Test: `crates/cli/src/commands/upgrade.rs`（clap 解析 + 冲突交互逻辑）

**Interfaces:**
- Consumes: `upgrade_skill(paths, id, yes)` / `upgrade_all(paths, yes)` / `UpgradeReport` / `SkillkitError::UpgradeBlocked`（Task 4）
- Produces: `UpgradeCmd`（clap Args：`id: Option<String>` + `--all` + `--yes` + `--json`）

- [ ] **Step 1: 写失败测试**

创建 `crates/cli/src/commands/upgrade.rs`：

```rust
//! upgrade 子命令：skillkit upgrade <id> | --all [--yes] [--json]。
//! 冲突时 core 返回 UpgradeBlocked，人类模式打印受影响项目并 y/n 交互确认；--json 输出错误 JSON。
use clap::Args;
use skillkit_core::{paths::Paths, SkillkitError};

#[derive(Args)]
pub struct UpgradeCmd {
    /// skill id（升级单个；与 --all 互斥）
    id: Option<String>,
    /// 升级 registry 全部 skill（跳过 unmanaged / 未安装）
    #[arg(long)]
    all: bool,
    /// 跳过冲突确认
    #[arg(long)]
    yes: bool,
    /// JSON 输出
    #[arg(long)]
    json: bool,
}

pub fn run(cmd: UpgradeCmd) -> anyhow::Result<()> {
    let paths = Paths::production();
    match (cmd.id, cmd.all) {
        (Some(id), false) => run_one(&paths, &id, cmd.yes, cmd.json),
        (None, true) => run_all(&paths, cmd.yes, cmd.json),
        (None, false) => anyhow::bail!("upgrade 需指定 <id> 或 --all"),
        (Some(_), true) => anyhow::bail!("<id> 与 --all 互斥"),
    }
}

fn run_one(paths: &Paths, id: &str, yes: bool, json: bool) -> anyhow::Result<()> {
    match skillkit_core::upgrade_skill(paths, id, yes) {
        Ok(r) => {
            if json {
                println!("{}", serde_json::to_string_pretty(&r)?);
            } else {
                println!(
                    "✓ 已升级 {} {} → {}",
                    r.id,
                    short(&r.old_hash),
                    short(&r.new_hash)
                );
                for p in &r.affected_projects {
                    println!("  ⚠ 项目 {p} 需重新 apply");
                }
            }
            Ok(())
        }
        Err(SkillkitError::UpgradeBlocked { id, affected }) => {
            if json {
                println!(
                    "{}",
                    serde_json::json!({"error": "upgrade_blocked", "id": id, "affected": affected})
                );
                std::process::exit(1);
            }
            println!(
                "⚠ 升级 {id} 将影响 {} 个项目（{}），它们的版本基线会漂移",
                affected.len(),
                affected.join(", ")
            );
            print!("确认升级？[y/N] ");
            std::io::Write::flush(&mut std::io::stdout())?;
            let mut line = String::new();
            std::io::stdin().read_line(&mut line)?;
            if line.trim().eq_ignore_ascii_case("y") {
                run_one(paths, &id, true, json)
            } else {
                anyhow::bail!("已取消升级 {id}");
            }
        }
        Err(e) => Err(e.into()),
    }
}

fn run_all(paths: &Paths, yes: bool, json: bool) -> anyhow::Result<()> {
    let reports = skillkit_core::upgrade_all(paths, yes)?;
    if json {
        println!("{}", serde_json::to_string_pretty(&reports)?);
    } else {
        println!("已升级 {} 个 skill", reports.len());
        for r in &reports {
            println!("  ✓ {} {} → {}", r.id, short(&r.old_hash), short(&r.new_hash));
            for p in &r.affected_projects {
                println!("    ⚠ 项目 {p} 需重新 apply");
            }
        }
    }
    Ok(())
}

fn short(h: &str) -> &str {
    h.get(..8).unwrap_or(h)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn upgrade_parses_id_and_flags() {
        let cmd = UpgradeCmd::try_parse_from(["skillkit", "upgrade", "dc/foo", "--yes", "--json"]).unwrap();
        assert_eq!(cmd.id.as_deref(), Some("dc/foo"));
        assert!(!cmd.all);
        assert!(cmd.yes);
        assert!(cmd.json);
    }

    #[test]
    fn upgrade_parses_all() {
        let cmd = UpgradeCmd::try_parse_from(["skillkit", "upgrade", "--all"]).unwrap();
        assert!(cmd.all);
        assert!(cmd.id.is_none());
    }

    #[test]
    fn upgrade_rejects_missing_target() {
        let cmd = UpgradeCmd::try_parse_from(["skillkit", "upgrade"]).unwrap();
        // 无 id 也无 --all：run 时手工校验报错
        assert!(cmd.id.is_none());
        assert!(!cmd.all);
    }
}
```

（需要 `use clap::Args` 里的 `try_parse_from`——`Args` trait 自带 `try_parse_from`。检查 `#[derive(Args)]` 导出。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p skillkit-cli upgrade`
Expected: 编译失败（`UpgradeCmd` 未定义）

- [ ] **Step 3: 接线 main.rs（解开 Task 5 留的注释）+ mod.rs**

`crates/cli/src/commands/mod.rs`（若 Task 5 已加则确认有）：

```rust
pub mod import;
pub mod upgrade;
```

`crates/cli/src/main.rs` 确认 `Cmd::Upgrade(UpgradeCmd)` 分支 + `use commands::upgrade::{run as run_upgrade, UpgradeCmd};`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p skillkit-cli`
Expected: 3 个 upgrade 解析测试 + 2 个 import 解析测试全 PASS

- [ ] **Step 5: 跑 lint + 全量**

Run: `make format && cargo clippy -p skillkit-cli --all-targets -- -D warnings && cargo test -p skillkit-cli`
Expected: 零 warning + 全 PASS

- [ ] **Step 6: Commit**

```bash
git add crates/cli/src/commands/upgrade.rs crates/cli/src/commands/mod.rs crates/cli/src/main.rs
git commit -m "feat(cli): upgrade 命令（<id>|--all，冲突交互确认，--yes/--json）"
```

---

### Task 7: GUI Skills 视图 unmanaged 角标 + 升级按钮 + upgrade 端点

**Files:**
- Modify: `crates/server/src/routes/skills.rs`
- Modify: `crates/server/src/routes/mod.rs`
- Modify: `crates/server/templates/fragments/skills_main.html`
- Test: `crates/server/tests/routes.rs`（契约测试）

**Interfaces:**
- Consumes: `upgrade_skill(paths, id, yes)`（Task 4）
- Produces: `POST /{token}/skills/{id}/upgrade` handler

- [ ] **Step 1: 写失败测试（upgrade 端点契约）**

在 `crates/server/tests/routes.rs` 末尾追加（照 `skill_uninstall_removes_from_registry` 同款模式：tempdir + 直接注册 registry + oneshot POST）：

```rust
#[tokio::test]
async fn skill_upgrade_endpoint_returns_500_on_unmanaged() {
    // unmanaged skill（computed_hash=None）无法升级，端点应返回 500 且不 panic。
    let dir = tempfile::tempdir().unwrap();
    let state = skillkit_server::AppState {
        paths: skillkit_core::Paths::new(dir.path().to_path_buf()),
        token: "test-token".into(),
    };
    let mut reg = skillkit_core::Registry::default();
    reg.skills.insert(
        "unmanaged/foo".into(),
        skillkit_core::registry::SkillMeta {
            id: "unmanaged/foo".into(),
            name: "foo".into(),
            source: "unmanaged".into(),
            scope: skillkit_core::Scope::Global,
            version: None,
            computed_hash: None,
            installed_at: "2026-07-31".into(),
            canonical_path: dir
                .path()
                .join(".agents/skills/foo")
                .to_string_lossy()
                .into_owned(),
        },
    );
    reg.save(&state.paths).unwrap();

    let app = skillkit_server::app(state);
    let resp = app
        .oneshot(
            Request::builder()
                .method("POST")
                .uri("/test-token/skills/unmanaged%2Ffoo/upgrade")
                .body(Body::empty())
                .unwrap(),
        )
        .await
        .unwrap();
    assert_eq!(resp.status(), StatusCode::INTERNAL_SERVER_ERROR);
}

#[tokio::test]
async fn skill_upgrade_endpoint_returns_404_on_unknown() {
    // 未安装 skill upgrade → SkillNotInstalled → 500（core 报错，handler 不 panic）
    let dir = tempfile::tempdir().unwrap();
    let state = skillkit_server::AppState {
        paths: skillkit_core::Paths::new(dir.path().to_path_buf()),
        token: "test-token".into(),
    };
    let app = skillkit_server::app(state);
    let resp = app
        .oneshot(
            Request::builder()
                .method("POST")
                .uri("/test-token/skills/nope%2Fx/upgrade")
                .body(Body::empty())
                .unwrap(),
        )
        .await
        .unwrap();
    assert_eq!(resp.status(), StatusCode::INTERNAL_SERVER_ERROR);
}
```

（manager skill 的 upgrade 成功路径需要真跑 npx update，集成测试不覆盖——由 core 的 `#[ignore]` 端到端 Task 4 Step 6 覆盖。GUI 端点测试只验证错误路径不 panic + 路由可达。）

- [ ] **Step 2: skills.rs 加 upgrade handler**

`crates/server/src/routes/skills.rs` 末尾加：

```rust
pub async fn upgrade(
    State(state): State<AppState>,
    Path((token, id)): Path<(String, String)>,
) -> Response {
    // GUI 场景已显式点击升级，yes=true 不二次确认
    match skillkit_core::upgrade_skill(&state.paths, &id, true) {
        Ok(_) => render_skills(state, token, false),
        Err(e) => {
            tracing::error!(error = ?e, "upgrade 失败：{id}");
            StatusCode::INTERNAL_SERVER_ERROR.into_response()
        }
    }
}
```

- [ ] **Step 3: mod.rs 加路由**

`crates/server/src/routes/mod.rs` 在 skills 路由处追加：

```rust
        .route("/{token}/skills/{id}/upgrade", post(skills::upgrade))
```

（放在 `.route("/{token}/skills/{id}", delete(skills::uninstall))` 之后。）

- [ ] **Step 4: skills_main.html 加 unmanaged 角标 + 升级按钮**

`crates/server/templates/fragments/skills_main.html` 改为：

```html
<h1>Skills</h1>
<table class="data">
  <thead><tr><th>id</th><th>scope</th><th>source</th><th>version</th><th>computed_hash</th><th>ops</th></tr></thead>
  <tbody>
  {% for (s, enc) in skills %}
  <tr>
    <td>{{ s.id }}{% if s.computed_hash.is_none() %} <span class="badge">unmanaged</span>{% endif %}</td>
    <td>{{ s.scope }}</td>
    <td>{{ s.source }}</td>
    <td>{{ s.version.as_deref().unwrap_or("-") }}</td>
    <td>{{ s.computed_hash.as_deref().unwrap_or("-") }}</td>
    <td>
      {% if s.computed_hash.is_some() %}
      <button class="u" hx-post="/{{ token }}/skills/{{ enc }}/upgrade" hx-target="body" hx-swap="outerHTML">upgrade</button>
      {% endif %}
      <button class="x" hx-delete="/{{ token }}/skills/{{ enc }}" hx-target="body" hx-swap="outerHTML">×</button>
    </td>
  </tr>
  {% endfor %}
  </tbody>
</table>
```

（`app.css` 可加 `.badge` 浅色样式，非必需——若加，补一条 CSS 规则到 `crates/server/static/app.css`。）

- [ ] **Step 5: 跑测试确认通过（Askama 编译 + 契约测试）**

Run: `cargo test -p skillkit-server`
Expected: 新增 upgrade 契约测试 + 现有 19 个测试全 PASS（Askama 模板语法错误会被编译期捕获）

- [ ] **Step 6: 跑 lint + make check**

Run: `make format && make lint && cargo test -p skillkit-server`
Expected: 全绿

- [ ] **Step 7: Commit**

```bash
git add crates/server/src/routes/skills.rs crates/server/src/routes/mod.rs crates/server/templates/fragments/skills_main.html crates/server/tests/routes.rs crates/server/static/app.css
git commit -m "feat(server): Skills 视图 unmanaged 角标 + 升级按钮 + upgrade 端点"
```

---

### Task 8: mac-config justfile 加 install_skillkit

**Files:**
- Modify: `/Users/mywo/lab/mac-config/justfile`
- 注意：这是独立仓库（`/Users/mywo/lab/mac-config`），commit 在那里单独提交

- [ ] **Step 1: 读 justfile 确认追加位置**

Run: `grep -n "install_" /Users/mywo/lab/mac-config/justfile | head`
Expected: 看到 `install_skvm` / `install_opencli` 等 recipe 的段落

- [ ] **Step 2: 加 recipe**

在 `justfile` 的 install 段（`install_skvm` 之后）加：

```make
# install/update skillkit（本地 workspace，未发布前不进 Brewfile）
install_skillkit:
	cargo install --path /Users/mywo/lab/skillkit/crates/cli --locked --force
```

- [ ] **Step 3: 验证 recipe 语法**

Run: `cd /Users/mywo/lab/mac-config && just --list | grep skillkit`
Expected: 输出 `install_skillkit`

- [ ] **Step 4: Commit（在 mac-config 仓库）**

```bash
cd /Users/mywo/lab/mac-config
git add justfile
git commit -m "feat(justfile): install_skillkit——安装/更新 skillkit CLI（cargo install --path）"
```

---

### Task 9: 文档同步 + 全量收尾

**Files:**
- Modify: `docs/2026-07-29-skillkit-design.md`（§11 命令表）
- Modify: `docs/sessions/2026-07-29-skillkit-design.md`（§1.1 命令表面 + §5 待办）

- [ ] **Step 1: spec §11 命令表加 import-existing / upgrade**

`docs/2026-07-29-skillkit-design.md` 的 §11 CLI 命令的「迁移」段与「skill 安装」段更新：

```markdown
# skill 安装到 canonical
skillkit install add <source> <skill> [--scope global|local]   # global→symlink ~/.agents/skills/, local→入池子等 project apply
skillkit uninstall <id>
skillkit upgrade <id> | --all [--yes] [--json]  # npx skills update + 重读 computed_hash；冲突列受影响项目，--yes 跳过
skillkit list [--scope global|local] [--json]

# 迁移
skillkit import-existing [--json] [--dry-run]   # 扫描存量 skill 目录，可溯重装入池 + 无源 unmanaged 登记
```

- [ ] **Step 2: 交接记录 §1.1 + §5 更新**

`docs/sessions/2026-07-29-skillkit-design.md`：
- §1.1 命令表面补 `skillkit import-existing` / `skillkit upgrade` 两行。
- §5 待办把「M3 迁移打磨」三项标为已完成（import-existing / upgrade / justfile）。

- [ ] **Step 3: 全量验证**

Run: `make check`
Expected: 全绿（core 34+ 新测试 + cli 3+5 新测试 + server 19+1 + clippy 零 warning）

Run: `cargo test -p skillkit-core -- --ignored`
Expected: m0 2 个 + m3 1 个端到端全过

- [ ] **Step 4: Commit**

```bash
git add docs/2026-07-29-skillkit-design.md docs/sessions/2026-07-29-skillkit-design.md
git commit -m "docs: M3 完成——命令表面补 import-existing/upgrade，交接记录更新"
```

---

## 完成标准

- `make check` 全绿（含 clippy `-D warnings` 零 warning）。
- `cargo test -p skillkit-core -- --ignored`：m0 2 个 + m3 1 个端到端真跑 npx skills 全过。
- 手工：`skillkit import-existing --dry-run` 在真实环境列出存量（63 agents / 9 codex / 2 cursor / claude 真实目录），`--json` 输出结构化；`skillkit upgrade <id> --yes` 更新 hash；GUI Skills 视图 unmanaged skill 带角标、无升级按钮。
- mac-config `just install_skillkit` 能把 `skillkit` 装进 PATH。
